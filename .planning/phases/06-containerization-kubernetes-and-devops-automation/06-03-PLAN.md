---
phase: 06-containerization-kubernetes-and-devops-automation
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - k8s/namespace.yaml
  - k8s/configmap.yaml
  - k8s/secrets.yaml
  - k8s/pvc.yaml
  - k8s/deployment-jobot.yaml
  - k8s/deployment-ollama.yaml
  - k8s/service.yaml
  - k8s/ingress.yaml
  - tests/test_devops.py
autonomous: true
requirements:
  - REQ-OPS-03
user_setup: []

must_haves:
  truths:
    - "Kubernetes manifests declare all infrastructure components in the dedicated jobot namespace"
    - "PersistentVolumeClaims guarantee durable storage for SQLite/Chroma and Ollama models"
    - "Jobot deployment specifies health probes and single-replica constraint to preserve SQLite integrity"
  artifacts:
    - "k8s/namespace.yaml"
    - "k8s/configmap.yaml"
    - "k8s/secrets.yaml"
    - "k8s/pvc.yaml"
    - "k8s/deployment-jobot.yaml"
    - "k8s/deployment-ollama.yaml"
    - "k8s/service.yaml"
    - "k8s/ingress.yaml"
  key_links:
    - "k8s/deployment-jobot.yaml mounts k8s/pvc.yaml and reads k8s/configmap.yaml"
---

<objective>
Implement the complete suite of Kubernetes manifests for Jobot and Ollama.

Purpose: Enable deploying Jobot to private cloud or home Kubernetes clusters (k3s, Minikube, MicroK8s, EKS, GKE) with persistent storage and ingress routing.
Output: Complete `k8s/` manifest directory and validation tests.
</objective>

<threat_model>
- **T-07 (Network Isolation & Secret Protection)**: Restrict inter-service traffic within `jobot` namespace and manage cloud API keys via Kubernetes Secrets.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kubernetes Manifests in k8s/</name>
  <files>k8s/namespace.yaml, k8s/configmap.yaml, k8s/secrets.yaml, k8s/pvc.yaml, k8s/deployment-jobot.yaml, k8s/deployment-ollama.yaml, k8s/service.yaml, k8s/ingress.yaml</files>
  <action>
    Create `k8s/` directory and author manifests:
    1. `k8s/namespace.yaml`: Namespace `jobot`.
    2. `k8s/configmap.yaml`: ConfigMap `jobot-config` (`APP_NAME`, `LOG_LEVEL`, `OLLAMA_BASE_URL=http://ollama-service.jobot.svc.cluster.local:11434`, `OLLAMA_MODEL=qwen2.5:7b`, `OLLAMA_EMBED_MODEL=nomic-embed-text`, `DATABASE_URL=sqlite:///data/jobot.db`, `CHROMA_DIR=data/chroma`).
    3. `k8s/secrets.yaml`: Secret `jobot-secrets` (`OPENAI_API_KEY=""`).
    4. `k8s/pvc.yaml`:
       - `jobot-data-pvc`: 5Gi, ReadWriteOnce.
       - `ollama-models-pvc`: 20Gi, ReadWriteOnce.
    5. `k8s/deployment-jobot.yaml`:
       - Deployment `jobot-deployment` (1 replica).
       - Container `jobot` image `jobot:latest` (imagePullPolicy `IfNotPresent`).
       - Volume mount `/app/data` to `jobot-data-pvc`.
       - `livenessProbe` and `readinessProbe` to `/health` on port 8000.
       - EnvFrom `jobot-config` and `jobot-secrets`.
    6. `k8s/deployment-ollama.yaml`:
       - Deployment `ollama-deployment` (1 replica).
       - Container `ollama` image `ollama/ollama:latest`.
       - Volume mount `/root/.ollama` to `ollama-models-pvc`.
       - Port 11434.
    7. `k8s/service.yaml`:
       - `jobot-service`: ClusterIP on port 8000 targeting container port 8000.
       - `ollama-service`: ClusterIP on port 11434 targeting container port 11434.
    8. `k8s/ingress.yaml`:
       - Ingress `jobot-ingress` routing host `jobot.local` / prefix `/` to `jobot-service:8000`.
  </action>
  <verify>python -c "import glob; assert len(glob.glob('k8s/*.yaml')) == 8; print('K8s manifests OK')"</verify>
  <acceptance_criteria>
    - All 8 Kubernetes YAML manifests exist and declare resources in namespace jobot
  </acceptance_criteria>
  <done>Kubernetes manifest directory created with all resource definitions</done>
</task>

<task type="auto">
  <name>Task 2: Add Kubernetes Manifest Validation Tests to tests/test_devops.py</name>
  <files>tests/test_devops.py</files>
  <action>
    Update `tests/test_devops.py`:
    - Add `test_k8s_manifests()`: Parses all YAML files in `k8s/`, verifies `apiVersion`, `kind`, `metadata.namespace`, volume mounts, and probe configurations.
  </action>
  <verify>pytest tests/test_devops.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_devops.py pass cleanly
  </acceptance_criteria>
  <done>Kubernetes manifest tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_devops.py` passes with zero errors
</verification>

<success_criteria>
- Complete Kubernetes manifests implemented and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-containerization-kubernetes-and-devops-automation/06-03-SUMMARY.md`
</output>
