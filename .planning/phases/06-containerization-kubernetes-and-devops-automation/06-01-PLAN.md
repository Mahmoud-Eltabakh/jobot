---
phase: 06-containerization-kubernetes-and-devops-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - tests/test_devops.py
autonomous: true
requirements:
  - REQ-OPS-01
user_setup: []

must_haves:
  truths:
    - "Production Dockerfile builds a self-contained image with Python 3.11, all dependencies, and Playwright Chromium pre-installed"
    - "Runs under non-root appuser with restricted permissions"
    - "Includes a HEALTHCHECK instruction polling /health"
  artifacts:
    - "Dockerfile"
    - ".dockerignore"
    - "tests/test_devops.py"
  key_links:
    - "Dockerfile entrypoint executes uvicorn app.main:app on port 8000"
---

<objective>
Implement the production multi-stage Dockerfile and optimized .dockerignore.

Purpose: Package Jobot into an immutable, secure, production-grade container image containing all Python libraries and headless browser dependencies.
Output: Production Dockerfile, updated .dockerignore, and DevOps validation test suite.
</objective>

<threat_model>
- **T-05 (Container Security & Non-Root Execution)**: Enforce running under non-root UID 10001 (`appuser`) with least-privilege file permissions on `/app/data`.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Production Dockerfile</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create `Dockerfile`:
    - Base on `python:3.11-slim`.
    - Install essential system libraries for Playwright Chromium (`libnss3`, `libatk1.0-0`, `libcups2`, `libdrm2`, `libgbm1`, `libpango-1.0-0`, `libcairo2`, `libasound2`, `fonts-liberation`, `ca-certificates`).
    - Create non-root system user `appuser` (UID 10001).
    - Create working directory `/app` and `/app/data` with ownership `appuser:appuser`.
    - Copy requirements.txt, install dependencies, and run `playwright install chromium`.
    - Copy application source code.
    - Set environment variables (`PYTHONUNBUFFERED=1`, `PORT=8000`, `HOST=0.0.0.0`).
    - Add `HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 CMD curl -f http://localhost:8000/health || exit 1`.
    - Switch to `USER appuser`.
    - Expose port 8000 and set `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`.
    
    Update `.dockerignore` to ensure `.git`, `.venv`, `data/`, `tests/`, and cache directories are excluded from image context.
  </action>
  <verify>python -c "assert open('Dockerfile').read().find('appuser') != -1; print('Dockerfile OK')"</verify>
  <acceptance_criteria>
    - Dockerfile contains non-root USER directive, HEALTHCHECK, and playwright installation
    - .dockerignore excludes virtual environments and local data directories
  </acceptance_criteria>
  <done>Production Dockerfile and .dockerignore implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement DevOps Test Suite in tests/test_devops.py</name>
  <files>tests/test_devops.py</files>
  <action>
    Create `tests/test_devops.py`:
    - `test_dockerfile_syntax()`: Asserts `Dockerfile` and `Dockerfile.dev` exist, verify base images, non-root user configuration, and `playwright install` directive.
    - `test_dockerignore_rules()`: Asserts `.dockerignore` excludes `.venv`, `data/`, `__pycache__`.
  </action>
  <verify>pytest tests/test_devops.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_devops.py pass cleanly
  </acceptance_criteria>
  <done>DevOps test suite created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_devops.py` passes with zero errors
</verification>

<success_criteria>
- Production Dockerfile, .dockerignore, and validation tests completed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-containerization-kubernetes-and-devops-automation/06-01-SUMMARY.md`
</output>
