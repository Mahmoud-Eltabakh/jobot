---
phase: 06-containerization-kubernetes-and-devops-automation
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - scripts/build.sh
  - scripts/build.ps1
  - scripts/run-docker.sh
  - scripts/run-docker.ps1
  - scripts/deploy-k8s.sh
  - scripts/deploy-k8s.ps1
  - tests/test_devops.py
autonomous: true
requirements:
  - REQ-OPS-04
user_setup: []

must_haves:
  truths:
    - "Automation scripts are provided for both Bash (.sh) and PowerShell (.ps1)"
    - "Scripts handle image building, local Docker Compose execution, automatic Ollama model pre-pulling, and ordered Kubernetes deployment"
  artifacts:
    - "scripts/build.sh"
    - "scripts/build.ps1"
    - "scripts/run-docker.sh"
    - "scripts/run-docker.ps1"
    - "scripts/deploy-k8s.sh"
    - "scripts/deploy-k8s.ps1"
  key_links:
    - "scripts invoke docker, docker-compose, and kubectl CLI tools"
---

<objective>
Implement cross-platform automation scripts in Bash and PowerShell for building, running, model pre-pulling, and deploying Jobot.

Purpose: Allow developers and end-users to build images, launch local multi-container stacks, and deploy to Kubernetes with single CLI commands.
Output: Suite of 6 automation scripts in `scripts/` and validation tests.
</objective>

<threat_model>
- **T-03 (Script Command Injection)**: Use strict quoting and parameter validation across all shell and PowerShell scripts.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-RESEARCH.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-01-PLAN.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-02-PLAN.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Cross-Platform Scripts in scripts/</name>
  <files>scripts/build.sh, scripts/build.ps1, scripts/run-docker.sh, scripts/run-docker.ps1, scripts/deploy-k8s.sh, scripts/deploy-k8s.ps1</files>
  <action>
    Create `scripts/` directory and implement 6 automation scripts:
    1. `scripts/build.sh` & `scripts/build.ps1`:
       - Builds `jobot:latest` using `Dockerfile` and `jobot:dev` using `Dockerfile.dev`.
       - Supports `--dev` and `--prod` arguments.
    2. `scripts/run-docker.sh` & `scripts/run-docker.ps1`:
       - Starts `docker compose -f docker-compose.yml up -d` (or `--dev` for `docker-compose.dev.yml`).
       - Waits for Ollama service to become healthy on port 11434.
       - Triggers model pulls inside Ollama: `docker exec jobot-ollama-dev ollama pull qwen2.5:7b` and `docker exec jobot-ollama-dev ollama pull nomic-embed-text`.
       - Outputs confirmation with dashboard URL `http://localhost:8000`.
    3. `scripts/deploy-k8s.sh` & `scripts/deploy-k8s.ps1`:
       - Applies `k8s/` manifests in correct order: namespace -> configmap & secret -> pvc -> deployments -> service -> ingress.
       - Runs `kubectl rollout status deployment/jobot-deployment -n jobot`.
  </action>
  <verify>python -c "import glob; assert len(glob.glob('scripts/*.*')) == 6; print('Scripts OK')"</verify>
  <acceptance_criteria>
    - All 6 scripts exist, use strict error handling (set -e in bash / $ErrorActionPreference = 'Stop' in pwsh)
  </acceptance_criteria>
  <done>Automation scripts created for Bash and PowerShell</done>
</task>

<task type="auto">
  <name>Task 2: Add Scripts Validation Tests to tests/test_devops.py</name>
  <files>tests/test_devops.py</files>
  <action>
    Update `tests/test_devops.py`:
    - Add `test_scripts_exist_and_executable()`: Asserts all 6 scripts exist in `scripts/`, verify shebang lines in `.sh` files, and verify parameter parsing in `.ps1` files.
  </action>
  <verify>pytest tests/test_devops.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_devops.py pass cleanly
  </acceptance_criteria>
  <done>DevOps scripts test suite updated and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_devops.py` passes with zero errors
- [ ] `pytest` passes all tests across the workspace
</verification>

<success_criteria>
- Cross-platform build, run, model pull, and Kubernetes deployment automation scripts implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/06-containerization-kubernetes-and-devops-automation/06-04-SUMMARY.md`
</output>
