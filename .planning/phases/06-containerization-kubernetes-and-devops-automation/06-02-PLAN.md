---
phase: 06-containerization-kubernetes-and-devops-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - tests/test_devops.py
autonomous: true
requirements:
  - REQ-OPS-02
user_setup: []

must_haves:
  truths:
    - "Production docker-compose.yml defines both jobot and ollama services with persistent named volumes"
    - "Ollama service includes GPU reservation configuration with CPU fallback"
    - "Services communicate over an isolated bridge network with internal DNS resolution"
  artifacts:
    - "docker-compose.yml"
  key_links:
    - "docker-compose.yml links jobot to ollama:11434 with persistent storage volumes"
---

<objective>
Implement the production multi-container Docker Compose configuration.

Purpose: Provide a production-ready, one-command local/server stack coordinating Jobot and Ollama with persistent storage and GPU passthrough.
Output: Production docker-compose.yml and test assertions.
</objective>

<threat_model>
- **T-06 (Resource Starvation & Data Loss)**: Ensure named persistent volumes prevent SQLite and ChromaDB data loss during container updates.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-containerization-kubernetes-and-devops-automation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Production docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Create `docker-compose.yml`:
    - Define `jobot` service:
      - Build using production `Dockerfile`.
      - Expose port `8000:8000`.
      - Mount volume `jobot_data:/app/data`.
      - Environment variables: `APP_NAME=Jobot`, `ENV=production`, `DEBUG=false`, `DATABASE_URL=sqlite:///data/jobot.db`, `CHROMA_DIR=data/chroma`, `OLLAMA_BASE_URL=http://ollama:11434`, `OLLAMA_MODEL=qwen2.5:7b`, `OLLAMA_EMBED_MODEL=nomic-embed-text`.
      - `depends_on`: `ollama` (with condition `service_started`).
      - `restart: unless-stopped`.
    - Define `ollama` service:
      - Image `ollama/ollama:latest`.
      - Expose port `11434:11434`.
      - Mount volume `ollama_data:/root/.ollama`.
      - `restart: unless-stopped`.
      - Deploy reservations for GPU (`nvidia`, count `all`, capabilities `[gpu]`).
    - Define named volumes `jobot_data` and `ollama_data`.
    - Define custom bridge network `jobot_network`.
  </action>
  <verify>python -c "assert open('docker-compose.yml').read().find('jobot_data') != -1; print('docker-compose.yml OK')"</verify>
  <acceptance_criteria>
    - docker-compose.yml defines jobot and ollama services, persistent volumes, and GPU reservation
  </acceptance_criteria>
  <done>Production docker-compose.yml implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add Docker Compose Validation Tests to tests/test_devops.py</name>
  <files>tests/test_devops.py</files>
  <action>
    Update `tests/test_devops.py`:
    - Add `test_docker_compose_config()`: Parses `docker-compose.yml` and `docker-compose.dev.yml` asserting required services, environment keys, volume names, and port mappings.
  </action>
  <verify>pytest tests/test_devops.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_devops.py pass
  </acceptance_criteria>
  <done>Docker Compose verification tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_devops.py` passes with zero errors
</verification>

<success_criteria>
- Production docker-compose.yml created with volume persistence and GPU configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/06-containerization-kubernetes-and-devops-automation/06-02-SUMMARY.md`
</output>
