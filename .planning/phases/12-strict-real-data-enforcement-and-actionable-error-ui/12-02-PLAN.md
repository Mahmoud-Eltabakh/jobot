# Plan 12-02: Actionable UI Diagnostics & Alert Notification System

## Objective
Build a real-time actionable notification banner in the web dashboard for service outages and authentication requirements.

## Tasks
1. Create `templates/components/system_alerts.html` for actionable error banners (Ollama offline with re-test button, LinkedIn re-login required, scraper rate-limit notices).
2. Include alerts component in `templates/base.html` with HTMX live polling.
3. Implement `GET /web/components/system-alerts` in `app/web/routes.py`.
