# Plan 12-01: Runtime Mock Elimination & Strict Live Data Enforcement

## Objective
Remove all synthetic mock fallbacks from production scraping, profile analysis, and AI evaluation modules.

## Tasks
1. Refactor `app/scrapers/ai_extractor.py` and `app/scrapers/stepstone.py` to remove synthetic placeholder job generation on parsing errors.
2. Refactor `app/ai/linkedin_analyzer.py` to raise actionable exceptions on failed live profile extractions.
3. Update `app/ai/evaluator.py` and `app/ai/client.py` to handle offline LLM backends with explicit error propagation.
