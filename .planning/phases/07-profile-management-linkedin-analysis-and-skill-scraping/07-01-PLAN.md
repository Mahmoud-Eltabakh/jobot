# Plan 07-01: Profile Data Model & Vector Synchronization

## Objective
Enhance `UserProfile` schema in SQLite and connect profile persistence directly to ChromaDB vector re-embedding.

## Tasks
1. Update `UserProfile` in `app/db/models.py` with `headline`, `bio`, `experience_years`, `work_preference`, `active_search_skills_json`, `experience_history_json`, `education_json`, `linkedin_session_cookie`, and `linkedin_data_json`.
2. Enhance `ProfileEmbedder` in `app/ai/embeddings.py` to chunk headline, bio, experience history, and active skills into semantic vector collections.
3. Update `app/ai/profile_extractor.py` to handle full structured profiles.
4. Add `app/api/profile.py` with REST endpoints for profile retrieval and updating.
