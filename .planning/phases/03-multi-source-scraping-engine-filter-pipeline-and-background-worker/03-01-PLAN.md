---
phase: 03-multi-source-scraping-engine-filter-pipeline-and-background-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scrapers/__init__.py
  - app/scrapers/base.py
  - app/scrapers/dedup.py
  - tests/test_scraper_pipeline.py
autonomous: true
requirements:
  - REQ-SCR-03
  - REQ-DB-01
user_setup: []

must_haves:
  truths:
    - "Raw scraped jobs are normalized into ScrapedJob dataclass with title, company, location, url, salary, and description"
    - "Deduplication hash is computed deterministically using normalized SHA-256 preventing duplicates"
    - "New jobs are inserted into SQLite while existing dedup hashes are skipped"
  artifacts:
    - "app/scrapers/base.py"
    - "app/scrapers/dedup.py"
    - "tests/test_scraper_pipeline.py"
  key_links:
    - "app.scrapers.dedup uses app.db.models.Job to query and insert unique jobs"
---

<objective>
Implement the base scraping models, deduplication hashing engine, and SQLite persistence pipeline.

Purpose: Provide a unified data contract (`ScrapedJob`) for all scrapers (JobSpy, StepStone) and prevent duplicate job entries across multiple runs.
Output: Base scraper contracts, deduplication helper, and test suite.
</objective>

<threat_model>
- **T-03 (SQL Injection)**: Enforce SQLModel parameterized inserts and commits.
- **T-04 (SSRF / URL Sanitization)**: Validate job URLs match standard HTTP/HTTPS schemes.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ScrapedJob data contract in app/scrapers/base.py</name>
  <files>app/scrapers/__init__.py, app/scrapers/base.py</files>
  <action>
    Create `app/scrapers/base.py`:
    - Define `ScrapedJob` Pydantic model: `source` (str), `source_id` (Optional[str]), `title` (str), `company` (str), `location` (str), `is_remote` (bool), `salary_min` (Optional[float]), `salary_max` (Optional[float]), `salary_currency` (Optional[str]), `url` (str), `description` (str), `date_posted` (Optional[datetime]).
    - Define `BaseScraper` abstract class with async method `scrape(search_term: str, location: str, results_wanted: int = 25) -> list[ScrapedJob]`.
  </action>
  <verify>python -c "from app.scrapers.base import ScrapedJob, BaseScraper; print('Base scrapers imported')"</verify>
  <acceptance_criteria>
    - ScrapedJob model validates fields and defaults
  </acceptance_criteria>
  <done>ScrapedJob and BaseScraper interface implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deduplication Engine in app/scrapers/dedup.py</name>
  <files>app/scrapers/dedup.py</files>
  <action>
    Create `app/scrapers/dedup.py`:
    - `compute_dedup_hash(company: str, title: str, location: str) -> str`: Normalizes strings (lowercase, stripped, alphanumeric normalization) and generates SHA-256 hash.
    - `save_scraped_jobs(jobs: list[ScrapedJob], session: Session) -> tuple[list[Job], int]`: Queries existing `dedup_hash` records, inserts new unique `Job` records with `JobStatus.SEEN`, and returns inserted jobs list and duplicate count.
  </action>
  <verify>python -c "from app.scrapers.dedup import compute_dedup_hash, save_scraped_jobs; print('Dedup imported')"</verify>
  <acceptance_criteria>
    - compute_dedup_hash produces identical hashes for identical jobs regardless of casing or extra spaces
    - save_scraped_jobs inserts unique jobs and skips duplicates
  </acceptance_criteria>
  <done>Deduplication engine and SQLite persistence implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Pipeline Unit Tests in tests/test_scraper_pipeline.py</name>
  <files>tests/test_scraper_pipeline.py</files>
  <action>
    Create `tests/test_scraper_pipeline.py`:
    - Test `ScrapedJob` model instantiation and validation.
    - Test `compute_dedup_hash` normalization with variations in casing and whitespace.
    - Test `save_scraped_jobs` inserts new jobs and ignores existing duplicate hashes.
  </action>
  <verify>pytest tests/test_scraper_pipeline.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_scraper_pipeline.py pass cleanly
  </acceptance_criteria>
  <done>Scraper pipeline tests created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_scraper_pipeline.py` passes with zero errors
</verification>

<success_criteria>
- Base scraper interfaces, deterministic deduplication hashing, and database insertion pipeline implemented and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-01-SUMMARY.md`
</output>
