---
phase: 03-multi-source-scraping-engine-filter-pipeline-and-background-worker
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scrapers/stepstone.py
  - tests/test_stepstone.py
autonomous: true
requirements:
  - REQ-SCR-02
  - REQ-SCR-05
user_setup: []

must_haves:
  truths:
    - "StepStoneScraper uses Playwright headless Chromium to search stepstone.de / stepstone.com"
    - "Handles cookie consent popups, pagination, and extracts full job descriptions and salary badges"
    - "Implements jitter delays (2-5s) and browser context cleanup preventing detection"
  artifacts:
    - "app/scrapers/stepstone.py"
    - "tests/test_stepstone.py"
  key_links:
    - "app.scrapers.stepstone inherits from app.scrapers.base.BaseScraper and returns ScrapedJob"
---

<objective>
Implement the dedicated StepStone scraper using headless Playwright browser automation.

Purpose: Provide reliable job extraction for StepStone's dynamic layout, European/German listings, salary tags, and cookie banners.
Output: StepStone Playwright crawler and automated test suite.
</objective>

<threat_model>
- **T-06 (Anti-Bot / IP Ban)**: Use Playwright stealth headers, random user-agents, and jittered request delays.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-RESEARCH.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement StepStone Playwright Scraper in app/scrapers/stepstone.py</name>
  <files>app/scrapers/stepstone.py</files>
  <action>
    Create `app/scrapers/stepstone.py`:
    - Implement `StepStoneScraper(BaseScraper)`:
      - Launch async Playwright Chromium browser (`async_playwright()`).
      - Navigate to `https://www.stepstone.de/work/{search_term}-in-{location}`.
      - Handle cookie consent popup buttons if present.
      - Extract job listing cards from DOM (`article[data-testid="job-item"]` or fallback selectors).
      - Extract title, company, location, salary tags, job URL, and job description.
      - Apply randomized delays (1.5s–3.5s) between page interactions.
      - Return list of `ScrapedJob` records.
      - Ensure browser and context close properly in `finally` block.
  </action>
  <verify>python -c "from app.scrapers.stepstone import StepStoneScraper; print('StepStoneScraper imported')"</verify>
  <acceptance_criteria>
    - StepStoneScraper implements BaseScraper interface
    - Browser lifecycle is safely managed with context cleanup
  </acceptance_criteria>
  <done>StepStone Playwright crawler implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement StepStone Scraper Tests in tests/test_stepstone.py</name>
  <files>tests/test_stepstone.py</files>
  <action>
    Create `tests/test_stepstone.py`:
    - Test DOM parsing logic on sample StepStone HTML card fixtures.
    - Test field extraction (salary badges, remote tags, titles, links).
    - Test graceful handling of empty or blocked pages.
  </action>
  <verify>pytest tests/test_stepstone.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_stepstone.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for StepStone scraper</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_stepstone.py` passes with zero errors
</verification>

<success_criteria>
- StepStone Playwright scraping engine and DOM card parser implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-03-SUMMARY.md`
</output>
