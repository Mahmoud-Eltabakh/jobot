---
phase: 03-multi-source-scraping-engine-filter-pipeline-and-background-worker
plan: 05
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - app/scrapers/scheduler.py
  - app/api/scrapers.py
  - tests/test_scheduler.py
autonomous: true
requirements:
  - REQ-SCH-01
  - REQ-SCH-02
  - REQ-SCH-03
user_setup: []

must_haves:
  truths:
    - "APScheduler initializes on application startup and schedules background scrape jobs based on configured intervals"
    - "Background worker runs scraping -> deduplication -> pre-filtering -> AI evaluation pipeline sequentially"
    - "REST API exposes POST /api/scrapers/run endpoint allowing immediate on-demand scrape triggers from the web UI"
  artifacts:
    - "app/scrapers/scheduler.py"
    - "app/api/scrapers.py"
    - "tests/test_scheduler.py"
  key_links:
    - "app.scrapers.scheduler integrates app.scrapers.jobspy_scraper, app.scrapers.stepstone, app.scrapers.filter_pipeline, and app.ai.evaluator"
    - "app.main mounts app.api.scrapers and manages scheduler lifespan"
---

<objective>
Implement the APScheduler background worker, end-to-end pipeline orchestrator, and on-demand scrape REST API endpoints.

Purpose: Provide automated periodic job discovery and instant manual search triggers connecting scrapers, filters, database, and AI evaluation.
Output: Background scheduler service, scraper API router, and integration test suite.
</objective>

<threat_model>
- **T-06 (Scraper Overload / Denial of Service)**: Prevent concurrent overlapping scrape jobs using execution locks.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-RESEARCH.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-01-PLAN.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-02-PLAN.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-03-PLAN.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pipeline Orchestrator and Scheduler in app/scrapers/scheduler.py</name>
  <files>app/scrapers/scheduler.py</files>
  <action>
    Create `app/scrapers/scheduler.py`:
    - Implement `ScraperPipeline`:
      - `async run_full_pipeline(session: Session, ai_client: Optional[BaseAIClient] = None) -> dict[str, Any]`:
        1. Query active `SearchConfig` entries from database (or use default keywords/location from `UserProfile` / settings).
        2. Execute `JobSpyScraper` (LinkedIn, Google Jobs) and `StepStoneScraper`.
        3. Run raw results through `save_scraped_jobs` (deduplication).
        4. Run new jobs through `JobFilterPipeline.apply_filters_and_save()`.
        5. Trigger AI evaluation on approved new jobs using `JobEvaluator.evaluate_and_update_job()`.
        6. Return summary stats (`scraped_total`, `new_inserted`, `filtered_out`, `evaluated`).
    - Implement `JobotScheduler`:
      - Wrap `apscheduler.schedulers.asyncio.AsyncIOScheduler`.
      - Start and stop with FastAPI lifespan in `app/main.py`.
      - Expose `schedule_periodic_scrape(interval_hours: int)`.
  </action>
  <verify>python -c "from app.scrapers.scheduler import ScraperPipeline, JobotScheduler; print('Scheduler imported')"</verify>
  <acceptance_criteria>
    - ScraperPipeline coordinates extraction, deduplication, filtering, and AI evaluation
    - JobotScheduler manages AsyncIOScheduler jobs
  </acceptance_criteria>
  <done>Scraper pipeline orchestrator and APScheduler implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Scraper API Router in app/api/scrapers.py</name>
  <files>app/api/__init__.py, app/api/scrapers.py, app/main.py</files>
  <action>
    Create `app/api/scrapers.py`:
    - `POST /api/scrapers/run`: Triggers immediate scrape cycle in background and returns status.
    - `GET /api/scrapers/status`: Returns scheduler status, last run time, and active search configs.
    - Mount router in `app/main.py` under `/api`.
  </action>
  <verify>python -c "from app.api.scrapers import router; print('Scraper API router imported')"</verify>
  <acceptance_criteria>
    - POST /api/scrapers/run endpoint returns 200/202 status
  </acceptance_criteria>
  <done>REST API endpoints for scraper control created</done>
</task>

<task type="auto">
  <name>Task 3: Implement Scheduler & API Tests in tests/test_scheduler.py</name>
  <files>tests/test_scheduler.py</files>
  <action>
    Create `tests/test_scheduler.py`:
    - Test pipeline orchestration with mocked scrapers and AIClient.
    - Test API endpoints `POST /api/scrapers/run` and `GET /api/scrapers/status`.
  </action>
  <verify>pytest tests/test_scheduler.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_scheduler.py pass cleanly
  </acceptance_criteria>
  <done>Unit and integration tests for scheduler and API created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_scheduler.py` passes with zero errors
- [ ] `pytest` passes all Phase 1, Phase 2, and Phase 3 tests
</verification>

<success_criteria>
- End-to-end background scheduling, deduplication, pre-filtering, AI scoring pipeline, and on-demand REST API implemented and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-05-SUMMARY.md`
</output>
