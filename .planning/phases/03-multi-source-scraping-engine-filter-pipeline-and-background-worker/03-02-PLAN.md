---
phase: 03-multi-source-scraping-engine-filter-pipeline-and-background-worker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scrapers/jobspy_scraper.py
  - tests/test_scrapers.py
autonomous: true
requirements:
  - REQ-SCR-01
  - REQ-SCR-05
user_setup: []

must_haves:
  truths:
    - "JobSpyScraper wraps python-jobspy to extract jobs from LinkedIn and Google Jobs (plus optional boards)"
    - "Scraped listings are mapped into normalized ScrapedJob records with salary, remote status, and description"
    - "Scraper handles rate limiting, network timeouts, and empty results gracefully"
  artifacts:
    - "app/scrapers/jobspy_scraper.py"
    - "tests/test_scrapers.py"
  key_links:
    - "app.scrapers.jobspy_scraper inherits from app.scrapers.base.BaseScraper and returns ScrapedJob"
---

<objective>
Implement the multi-source scraper for LinkedIn and Google Jobs using `python-jobspy`.

Purpose: Provide robust job extraction from major job boards with built-in header rotation, rate-limit handling, and pagination.
Output: JobSpy scraper wrapper and automated test suite.
</objective>

<threat_model>
- **T-06 (Anti-Bot / Rate Limiting)**: Manage request intervals, respect status codes, and handle transient network failures without crashing.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-RESEARCH.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement JobSpyScraper in app/scrapers/jobspy_scraper.py</name>
  <files>app/scrapers/jobspy_scraper.py</files>
  <action>
    Create `app/scrapers/jobspy_scraper.py`:
    - Implement `JobSpyScraper(BaseScraper)`:
      - `async def scrape(search_term: str, location: str, results_wanted: int = 25, site_name: Optional[list[str]] = None, is_remote: bool = False, hours_old: int = 72) -> list[ScrapedJob]`:
      - Run `jobspy.scrape_jobs()` inside an asyncio thread pool (`asyncio.to_thread`) to avoid blocking the FastAPI event loop.
      - Map DataFrame columns (`site`, `title`, `company`, `location`, `job_url`, `description`, `min_amount`, `max_amount`, `currency`, `is_remote`, `date_posted`) to `ScrapedJob` objects.
      - Handle exceptions (e.g. timeout, rate limit, empty results) returning empty list with warning logs.
  </action>
  <verify>python -c "from app.scrapers.jobspy_scraper import JobSpyScraper; print('JobSpyScraper imported')"</verify>
  <acceptance_criteria>
    - JobSpyScraper normalizes DataFrame rows into ScrapedJob objects
    - Scraper runs asynchronously without blocking event loop
  </acceptance_criteria>
  <done>JobSpy scraper integration implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Scraper Tests in tests/test_scrapers.py</name>
  <files>tests/test_scrapers.py</files>
  <action>
    Create `tests/test_scrapers.py`:
    - Mock `jobspy.scrape_jobs` returning sample DataFrame records.
    - Test conversion of LinkedIn and Google Jobs DataFrame rows to `ScrapedJob`.
    - Test error handling when JobSpy encounters network exceptions.
  </action>
  <verify>pytest tests/test_scrapers.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_scrapers.py pass cleanly
  </acceptance_criteria>
  <done>JobSpy wrapper unit tests created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_scrapers.py` passes with zero errors
</verification>

<success_criteria>
- LinkedIn & Google Jobs extraction via JobSpy implemented and verified with mock tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-02-SUMMARY.md`
</output>
