---
phase: 03-multi-source-scraping-engine-filter-pipeline-and-background-worker
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/scrapers/filter_pipeline.py
  - tests/test_filter_pipeline.py
autonomous: true
requirements:
  - REQ-SCR-04
user_setup: []

must_haves:
  truths:
    - "Pre-filter pipeline evaluates scraped jobs against active FilterRule records before AI evaluation"
    - "Jobs matching blacklisted titles, companies, or negative keywords are flagged and prevented from consuming LLM tokens"
    - "Supports plain substring and regex matching modes"
  artifacts:
    - "app/scrapers/filter_pipeline.py"
    - "tests/test_filter_pipeline.py"
  key_links:
    - "app.scrapers.filter_pipeline queries app.db.models.FilterRule and updates app.db.models.Job"
---

<objective>
Implement the Title & Keyword Pre-Filter Pipeline to filter out unwanted jobs before AI evaluation.

Purpose: Drop or auto-reject non-relevant jobs based on user blacklist rules (e.g., "Director", "Lead", "PHP", "No Remote") to save LLM tokens and compute.
Output: Filter pipeline module and comprehensive test suite.
</objective>

<threat_model>
- **T-01 (Token & Compute DoS)**: Filter spam and mismatched jobs early before hitting LLM inference endpoints.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-RESEARCH.md
@.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pre-Filter Pipeline in app/scrapers/filter_pipeline.py</name>
  <files>app/scrapers/filter_pipeline.py</files>
  <action>
    Create `app/scrapers/filter_pipeline.py`:
    - Define `FilterResult` dataclass: `is_filtered: bool`, `matched_rule: Optional[FilterRule]`, `reason: Optional[str]`.
    - Implement `JobFilterPipeline`:
      - `evaluate_job(job: ScrapedJob | Job, active_rules: list[FilterRule]) -> FilterResult`:
        - Checks title against rules where `rule_type == 'title'`.
        - Checks company against rules where `rule_type == 'company'`.
        - Checks description against rules where `rule_type == 'keyword'`.
        - Supports regex when `rule.is_regex is True` and case-insensitive substring search otherwise.
      - `apply_filters_and_save(jobs: list[Job], session: Session) -> tuple[list[Job], list[Job]]`:
        - Splits jobs into `approved_for_ai` and `filtered_out`.
        - For filtered jobs: updates `status = JobStatus.NOT_A_FIT.value` and records reason in `fit_summary`.
  </action>
  <verify>python -c "from app.scrapers.filter_pipeline import JobFilterPipeline; print('Filter pipeline imported')"</verify>
  <acceptance_criteria>
    - JobFilterPipeline correctly detects title, company, and keyword blacklist matches
    - Filtered jobs are marked as NOT_A_FIT without throwing exceptions
  </acceptance_criteria>
  <done>Pre-filter pipeline implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Filter Pipeline Tests in tests/test_filter_pipeline.py</name>
  <files>tests/test_filter_pipeline.py</files>
  <action>
    Create `tests/test_filter_pipeline.py`:
    - Test title matching (case-insensitive substring and regex).
    - Test company matching.
    - Test description keyword matching.
    - Test database status update to `NOT_A_FIT`.
  </action>
  <verify>pytest tests/test_filter_pipeline.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_filter_pipeline.py pass cleanly
  </acceptance_criteria>
  <done>Filter pipeline unit tests created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_filter_pipeline.py` passes with zero errors
</verification>

<success_criteria>
- Rule-based title, company, and keyword pre-filter pipeline implemented and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-source-scraping-engine-filter-pipeline-and-background-worker/03-04-SUMMARY.md`
</output>
