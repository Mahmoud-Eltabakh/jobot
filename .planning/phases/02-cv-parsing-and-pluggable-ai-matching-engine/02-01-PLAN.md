---
phase: 02-cv-parsing-and-pluggable-ai-matching-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ai/__init__.py
  - app/ai/cv_parser.py
  - app/ai/profile_extractor.py
  - tests/test_cv_parser.py
autonomous: true
requirements:
  - REQ-CV-01
  - REQ-CV-02
  - REQ-CV-03
user_setup: []

must_haves:
  truths:
    - "PDF and DOCX CVs parse cleanly into structured text without layout scrambling"
    - "Files exceeding 5MB or invalid magic bytes are rejected with clear error codes"
    - "Candidate profile normalization produces validated ExtractedProfile Pydantic schema and updates UserProfile in SQLite"
  artifacts:
    - "app/ai/cv_parser.py"
    - "app/ai/profile_extractor.py"
    - "tests/test_cv_parser.py"
  key_links:
    - "app.ai.profile_extractor uses app.ai.cv_parser and app.ai.client to update app.db.models.UserProfile in SQLite"
---

<objective>
Implement secure PDF/DOCX CV text parsing and structured candidate profile extraction.

Purpose: Allow candidates to upload existing resumes, extracting structured skills, titles, and experience into the database for AI matching.
Output: CVParser, ProfileExtractor, and comprehensive unit tests.
</objective>

<threat_model>
- **T-02 (Malicious PDF / DoS)**: Enforce 5MB limit, magic byte validation (`%PDF-`), and safe parsing exceptions.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-RESEARCH.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-AI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PDF/DOCX Parser in app/ai/cv_parser.py</name>
  <files>app/ai/__init__.py, app/ai/cv_parser.py</files>
  <action>
    Create `app/ai/cv_parser.py`:
    - `CVParser` class with `extract_text_from_bytes(file_bytes: bytes, filename: str) -> str`.
    - Check magic bytes for PDF (`b"%PDF-"`) and validate file size <= 5MB. Raise ValueError if invalid or oversized.
    - Extract text using `pdfplumber` with fallback to `pypdf`.
    - Extract text for DOCX using `docx2txt` / `pypdf` with validation.
    - Clean whitespace and sanitize non-printable characters.
  </action>
  <verify>python -c "from app.ai.cv_parser import CVParser; print('CVParser imported')"</verify>
  <acceptance_criteria>
    - CVParser extracts text and rejects files > 5MB or invalid magic bytes with ValueError
  </acceptance_criteria>
  <done>CVParser implemented with security checks and text extraction</done>
</task>

<task type="auto">
  <name>Task 2: Implement Profile Extractor in app/ai/profile_extractor.py</name>
  <files>app/ai/profile_extractor.py</files>
  <action>
    Create `app/ai/profile_extractor.py`:
    - Define `ExtractedProfile` Pydantic model (`full_name`, `summary`, `skills: list[str]`, `experience_years: float`, `target_titles: list[str]`, `target_locations: list[str]`, `target_salary_min: Optional[float]`).
    - Implement `extract_profile_from_text(raw_text: str, ai_client: Any) -> ExtractedProfile`.
    - Implement fallback heuristic extractor if LLM is offline.
    - Implement `save_profile_to_db(profile: ExtractedProfile, raw_text: str, session: Session) -> UserProfile`.
  </action>
  <verify>python -c "from app.ai.profile_extractor import ExtractedProfile; print('ProfileExtractor imported')"</verify>
  <acceptance_criteria>
    - ExtractedProfile validates fields and converts to/from JSON
    - save_profile_to_db persists fields to UserProfile in SQLite
  </acceptance_criteria>
  <done>Structured profile extraction and database persistence implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Automated Tests in tests/test_cv_parser.py</name>
  <files>tests/test_cv_parser.py</files>
  <action>
    Create `tests/test_cv_parser.py`:
    - Test text extraction from valid PDF bytes.
    - Test oversized file (>5MB) rejection.
    - Test invalid file header rejection.
    - Test ExtractedProfile validation and database persistence.
  </action>
  <verify>pytest tests/test_cv_parser.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_cv_parser.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for CVParser and ProfileExtractor</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_cv_parser.py` passes with zero errors
</verification>

<success_criteria>
- Secure document parsing, structured profile extraction, and SQLite UserProfile persistence implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-01-SUMMARY.md`
</output>
