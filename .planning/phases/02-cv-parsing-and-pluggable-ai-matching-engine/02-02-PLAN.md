---
phase: 02-cv-parsing-and-pluggable-ai-matching-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - app/ai/embeddings.py
  - tests/test_embeddings.py
autonomous: true
requirements:
  - REQ-CV-04
  - REQ-DB-02
user_setup: []

must_haves:
  truths:
    - "Candidate profile chunks are embedded and upserted into ChromaDB cv_profile collection"
    - "Vector search calculates cosine similarity between job descriptions and candidate profile vectors"
  artifacts:
    - "app/ai/embeddings.py"
    - "tests/test_embeddings.py"
  key_links:
    - "app.ai.embeddings uses app.db.vector.VectorStore and app.ai.client.BaseAIClient"
---

<objective>
Implement semantic candidate profile chunking, embedding generation, and ChromaDB vector search helpers.

Purpose: Provide vector embeddings of candidate qualifications for instant semantic similarity scoring against incoming scraped jobs.
Output: ProfileEmbedder, similarity search functions, and test suite.
</objective>

<threat_model>
- **T-05 (Path Isolation)**: Validate ChromaDB persistent directory path.
- **T-01 (Vector Metadata Validation)**: Restrict metadata payloads to validated primitive types.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-RESEARCH.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-AI-SPEC.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-01-PLAN.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Profile Chunker & Embedder in app/ai/embeddings.py</name>
  <files>app/ai/embeddings.py</files>
  <action>
    Create `app/ai/embeddings.py`:
    - `ProfileEmbedder` class:
      - `chunk_profile(profile: ExtractedProfile) -> list[dict[str, Any]]`: Splits profile into Summary, Skills, and Experience chunks with metadata.
      - `async embed_and_store_profile(profile: ExtractedProfile, vector_store: VectorStore, ai_client: BaseAIClient) -> int`: Generates embeddings using `ai_client.embed()` and upserts into `VectorStore.COLLECTION_CV_PROFILE`.
      - `async compute_semantic_similarity(job_text: str, vector_store: VectorStore, ai_client: BaseAIClient) -> float`: Computes average cosine similarity score (0.0 to 1.0) between job description and candidate profile chunks.
  </action>
  <verify>python -c "from app.ai.embeddings import ProfileEmbedder; print('ProfileEmbedder imported')"</verify>
  <acceptance_criteria>
    - chunk_profile splits profile into summary, skills, and experience items
    - compute_semantic_similarity returns float between 0.0 and 1.0
  </acceptance_criteria>
  <done>Semantic chunking and embedding pipeline implemented</done>
</task>

<task type="auto">
  <name>Task 2: Test Profile Embedding Pipeline in tests/test_embeddings.py</name>
  <files>tests/test_embeddings.py</files>
  <action>
    Create `tests/test_embeddings.py`:
    - Test chunking logic for ExtractedProfile.
    - Test embedding storage and retrieval using temporary ChromaDB store and mocked AIClient.
    - Test similarity search calculation against sample job descriptions.
  </action>
  <verify>pytest tests/test_embeddings.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_embeddings.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for ProfileEmbedder</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_embeddings.py` passes with zero errors
- [ ] `pytest` passes all Phase 1 & Phase 2 tests
</verification>

<success_criteria>
- Semantic profile embedding, ChromaDB vector storage, and similarity search helpers operational and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-02-SUMMARY.md`
</output>
