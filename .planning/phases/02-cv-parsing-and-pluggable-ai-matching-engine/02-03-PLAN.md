---
phase: 02-cv-parsing-and-pluggable-ai-matching-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ai/client.py
  - app/ai/evaluator.py
  - tests/test_ai_client.py
  - tests/test_evaluator.py
autonomous: true
requirements:
  - REQ-AI-01
  - REQ-AI-02
  - REQ-AI-03
  - REQ-AI-04
user_setup: []

must_haves:
  truths:
    - "BaseAIClient unifies Local Ollama and OpenAI-compatible Cloud APIs behind async generate() and embed() methods"
    - "Job fit evaluation returns validated Pydantic JobFitEvaluation with 0-100% score and breakdown"
    - "Prompts isolate job descriptions with XML delimiters preventing prompt injection attacks"
  artifacts:
    - "app/ai/client.py"
    - "app/ai/evaluator.py"
    - "tests/test_ai_client.py"
    - "tests/test_evaluator.py"
  key_links:
    - "app.ai.evaluator uses app.ai.client and updates app.db.models.Job fit fields in SQLite"
---

<objective>
Implement pluggable multi-provider AI client (Ollama/OpenAI) and hardened job fit evaluation engine.

Purpose: Provide a unified AI abstraction supporting local offline Ollama models and cloud API keys, evaluating job descriptions against candidate profiles.
Output: BaseAIClient, OllamaAIClient, OpenAICompatibleClient, JobEvaluator, and test suites.
</objective>

<threat_model>
- **T-01 (Indirect Prompt Injection)**: System/user prompt isolation with XML delimiter fences (`<job_description>...</job_description>`) and strict Pydantic JSON parsing.
- **T-07 (Host Isolation)**: Validate API endpoint URLs.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-RESEARCH.md
@.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-AI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pluggable AI Client in app/ai/client.py</name>
  <files>app/ai/client.py</files>
  <action>
    Create `app/ai/client.py`:
    - `BaseAIClient` (abstract class):
      - `async generate(prompt: str, system_prompt: Optional[str] = None, json_mode: bool = True) -> str`
      - `async embed(text: str) -> list[float]`
      - `async check_health() -> bool`
    - `OllamaAIClient`: Powered by `ollama.AsyncClient`.
    - `OpenAICompatibleClient`: Powered by `openai.AsyncOpenAI`.
    - `get_ai_client(session: Optional[Session] = None) -> BaseAIClient`: Reads `ai_provider`, `ollama_base_url`, `ollama_model`, `openai_api_key`, etc. from `AppSettings` in the database.
  </action>
  <verify>python -c "from app.ai.client import get_ai_client, BaseAIClient; print('AI Client imported')"</verify>
  <acceptance_criteria>
    - BaseAIClient defines generate, embed, and check_health
    - get_ai_client resolves active provider from database
  </acceptance_criteria>
  <done>Multi-provider AI client abstraction implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Job Fit Evaluator in app/ai/evaluator.py</name>
  <files>app/ai/evaluator.py</files>
  <action>
    Create `app/ai/evaluator.py`:
    - Define `JobFitEvaluation` Pydantic model:
      - `fit_score`: int = Field(ge=0, le=100)
      - `fit_summary`: str
      - `pros`: list[str]
      - `cons`: list[str]
      - `missing_skills`: list[str]
      - `recommendation`: Literal["Strong Match", "Potential Match", "Not a Fit"]
    - Implement `JobEvaluator` with hardened prompt fencing (`<candidate_profile>`, `<job_description>`).
    - Implement `evaluate_job(candidate_summary: str, job_description: str, ai_client: BaseAIClient) -> JobFitEvaluation`.
    - Implement `evaluate_and_update_job(job_id: int, session: Session, ai_client: BaseAIClient) -> Job`: Updates `Job` record in database with score, pros, cons, and missing skills.
  </action>
  <verify>python -c "from app.ai.evaluator import JobEvaluator, JobFitEvaluation; print('JobEvaluator imported')"</verify>
  <acceptance_criteria>
    - JobFitEvaluation validates 0-100 score bounds
    - evaluate_and_update_job updates Job fields and commits to database
  </acceptance_criteria>
  <done>Job fit evaluator and database updater implemented</done>
</task>

<task type="auto">
  <name>Task 3: Automated Tests in tests/test_ai_client.py and tests/test_evaluator.py</name>
  <files>tests/test_ai_client.py, tests/test_evaluator.py</files>
  <action>
    Create `tests/test_ai_client.py` and `tests/test_evaluator.py`:
    - Test AI Client factory with mock settings.
    - Test JobEvaluator prompt generation, JSON response parsing, and schema validation.
    - Test database update of Job record fit score and reasons.
  </action>
  <verify>pytest tests/test_ai_client.py tests/test_evaluator.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_ai_client.py and tests/test_evaluator.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for AI client and evaluator</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_ai_client.py tests/test_evaluator.py` passes with zero errors
</verification>

<success_criteria>
- Pluggable AI Client and Job Evaluator implemented with XML prompt fencing and schema validation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cv-parsing-and-pluggable-ai-matching-engine/02-03-SUMMARY.md`
</output>
