---
phase: 05-feedback-loop-and-search-refinement-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ai/feedback.py
  - tests/test_feedback.py
autonomous: true
requirements:
  - REQ-FBL-01
  - REQ-FBL-02
user_setup: []

must_haves:
  truths:
    - "Status transitions to applied/interview generate positive vector embeddings in user_feedback collection"
    - "Status transitions to not a good fit/rejected with notes generate negative vector embeddings with penalty weights"
    - "JobEvaluator incorporates user feedback similarity scores to dynamically adjust overall match percentage"
  artifacts:
    - "app/ai/feedback.py"
    - "tests/test_feedback.py"
  key_links:
    - "app.ai.feedback uses app.db.vector.VectorStore and app.ai.client.BaseAIClient"
---

<objective>
Implement candidate feedback vector recording in ChromaDB and adaptive fit score re-weighting.

Purpose: Learn from user application outcomes and critique notes to progressively boost matching roles and penalize disliked patterns.
Output: Feedback manager module, score adjustment helpers, and test suite.
</objective>

<threat_model>
- **T-01 (Vector Metadata Validation)**: Restrict feedback vector metadata to primitive types and sanitized strings.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FeedbackManager in app/ai/feedback.py</name>
  <files>app/ai/feedback.py</files>
  <action>
    Create `app/ai/feedback.py`:
    - Implement `FeedbackManager`:
      - `async record_job_feedback(job_id: int, status: str, note_text: Optional[str], session: Session, vector_store: VectorStore, ai_client: BaseAIClient) -> Optional[str]`:
        - Classifies feedback sentiment (`positive` for applied/interview, `negative` for not_a_fit/rejected, `neutral` for seen).
        - Generates embedding vector for composite text (job title + company + note text).
        - Upserts document into `VectorStore.COLLECTION_USER_FEEDBACK` with metadata (`job_id`, `sentiment`, `status`, `created_at`).
      - `async compute_feedback_score_adjustment(job_text: str, vector_store: VectorStore, ai_client: BaseAIClient) -> tuple[int, list[str]]`:
        - Queries `user_feedback` collection for similar positive and negative vectors.
        - Calculates score delta: positive boost (up to +10%) or negative penalty (up to -25%).
        - Returns (adjusted_score_delta, list_of_feedback_reasons).
  </action>
  <verify>python -c "from app.ai.feedback import FeedbackManager; print('FeedbackManager imported')"</verify>
  <acceptance_criteria>
    - FeedbackManager records positive/negative vectors in ChromaDB
    - compute_feedback_score_adjustment returns integer score delta and reason strings
  </acceptance_criteria>
  <done>Feedback manager and vector re-weighting implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Feedback Unit Tests in tests/test_feedback.py</name>
  <files>tests/test_feedback.py</files>
  <action>
    Create `tests/test_feedback.py`:
    - Test recording positive and negative feedback vectors.
    - Test score delta computation for job descriptions similar to negative feedback notes.
    - Test score delta computation for job descriptions similar to positive applied roles.
  </action>
  <verify>pytest tests/test_feedback.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_feedback.py pass cleanly
  </acceptance_criteria>
  <done>Feedback manager unit tests created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_feedback.py` passes with zero errors
</verification>

<success_criteria>
- Feedback vector ingestion in ChromaDB and dynamic fit score re-weighting implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-loop-and-search-refinement-engine/05-01-SUMMARY.md`
</output>
