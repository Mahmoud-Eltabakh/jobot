---
phase: 05-feedback-loop-and-search-refinement-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - tests/test_e2e_workflow.py
autonomous: true
requirements:
  - REQ-AI-02
  - REQ-FBL-01
  - REQ-FBL-02
  - REQ-FBL-03
user_setup: []

must_haves:
  truths:
    - "Full lifecycle test validates scraping -> pre-filtering -> SQLite persistence -> ChromaDB vector embeddings -> LLM scoring -> candidate feedback -> adaptive re-ranking"
    - "Demonstrates that negative feedback on a position successfully reduces the fit score of subsequent similar jobs"
    - "Entire test suite runs cleanly in under 5 seconds"
  artifacts:
    - "tests/test_e2e_workflow.py"
  key_links:
    - "tests/test_e2e_workflow.py exercises app.scrapers, app.ai, app.db, and app.web"
---

<objective>
Implement comprehensive End-to-End integration test suite verifying the complete Jobot workflow.

Purpose: Guarantee that all architectural subsystems (Scrapers, Pre-filters, Relational DB, Vector Store, AI Evaluator, and Adaptive Feedback Loop) work together flawlessly.
Output: End-to-End integration test suite.
</objective>

<threat_model>
- **T-01 to T-07**: Verify all security protections operate in concert across the end-to-end data pipeline.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-RESEARCH.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-01-PLAN.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Full End-to-End Lifecycle Test in tests/test_e2e_workflow.py</name>
  <files>tests/test_e2e_workflow.py</files>
  <action>
    Create `tests/test_e2e_workflow.py`:
    - Set up isolated test database and temporary ChromaDB vector store.
    - Upload & parse candidate CV -> extract `UserProfile` -> embed in `cv_profile`.
    - Ingest batch of scraped jobs from LinkedIn & StepStone.
    - Run through `JobFilterPipeline`: verify blacklisted jobs are auto-flagged as `NOT_A_FIT`.
    - Run approved jobs through `JobEvaluator.evaluate_and_update_job()`: verify 0-100% scores and pros/cons.
    - Record negative feedback note on a job: verify `user_feedback` vector is created.
    - Evaluate a new incoming job with similar traits: verify adaptive score penalty is applied.
    - Run `BlacklistSuggester`: verify automatic blacklist rules are suggested from the negative feedback history.
  </action>
  <verify>pytest tests/test_e2e_workflow.py</verify>
  <acceptance_criteria>
    - Full end-to-end workflow test passes cleanly
  </acceptance_criteria>
  <done>End-to-end lifecycle integration test suite created and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_e2e_workflow.py` passes with zero errors
- [ ] `pytest` passes all tests across the entire repository
</verification>

<success_criteria>
- End-to-End system verification completed covering all functional requirements and feedback loops.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-loop-and-search-refinement-engine/05-03-SUMMARY.md`
</output>
