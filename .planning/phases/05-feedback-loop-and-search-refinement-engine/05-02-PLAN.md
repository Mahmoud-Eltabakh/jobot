---
phase: 05-feedback-loop-and-search-refinement-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/ai/blacklist_suggester.py
  - app/scrapers/query_optimizer.py
  - tests/test_blacklist_suggester.py
autonomous: true
requirements:
  - REQ-FBL-01
  - REQ-FBL-03
user_setup: []

must_haves:
  truths:
    - "System analyzes historical negative feedback comments to identify recurring rejection patterns"
    - "Suggests new FilterRule entries (title exclusions, negative keywords) for user approval"
    - "Query optimizer analyzes positive applied/interview positions to refine search keywords in SearchConfig"
  artifacts:
    - "app/ai/blacklist_suggester.py"
    - "app/scrapers/query_optimizer.py"
    - "tests/test_blacklist_suggester.py"
  key_links:
    - "app.ai.blacklist_suggester queries app.db.models.FeedbackNote and generates FilterRule candidates"
---

<objective>
Implement automatic blacklist rule suggestions and search query optimization.

Purpose: Turn candidate feedback and rejection themes into actionable blacklist rules and higher-yield search parameters.
Output: Blacklist suggester, search query optimizer, and test suite.
</objective>

<threat_model>
- **T-01 (Rule Validation)**: Ensure generated blacklist rules contain valid regex or clean alphanumeric patterns without breaking scraper queries.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-RESEARCH.md
@.planning/phases/05-feedback-loop-and-search-refinement-engine/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Blacklist Rule Suggester in app/ai/blacklist_suggester.py</name>
  <files>app/ai/blacklist_suggester.py</files>
  <action>
    Create `app/ai/blacklist_suggester.py`:
    - Define `SuggestedRule` Pydantic model: `rule_type: str`, `pattern: str`, `reason: str`, `frequency: int`.
    - Implement `BlacklistSuggester`:
      - `async def analyze_rejections_and_suggest_rules(session: Session, ai_client: Optional[BaseAIClient] = None) -> list[SuggestedRule]`:
        - Query negative `FeedbackNote` and `JobStatusHistory` records.
        - Extract frequent rejection keywords, unwanted job titles, and recurring negative phrases.
        - Use LLM or rule-based frequency counting to propose 2-5 actionable `FilterRule` candidates.
        - Exclude rules that already exist in the `FilterRule` table.
  </action>
  <verify>python -c "from app.ai.blacklist_suggester import BlacklistSuggester; print('BlacklistSuggester imported')"</verify>
  <acceptance_criteria>
    - BlacklistSuggester returns list of SuggestedRule objects derived from negative feedback
  </acceptance_criteria>
  <done>Automatic blacklist rule suggester implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Search Query Optimizer in app/scrapers/query_optimizer.py</name>
  <files>app/scrapers/query_optimizer.py</files>
  <action>
    Create `app/scrapers/query_optimizer.py`:
    - Implement `SearchQueryOptimizer`:
      - `def get_optimized_search_terms(session: Session) -> list[str]`:
        - Analyzes jobs in `applied`, `1. interview`, `2. interview`, `3. interview` statuses.
        - Extracts high-performing title tokens and skills.
        - Suggests refined search query strings for `SearchConfig`.
  </action>
  <verify>python -c "from app.scrapers.query_optimizer import SearchQueryOptimizer; print('QueryOptimizer imported')"</verify>
  <acceptance_criteria>
    - SearchQueryOptimizer returns optimized search strings based on positive job history
  </acceptance_criteria>
  <done>Search query optimizer implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Tests in tests/test_blacklist_suggester.py</name>
  <files>tests/test_blacklist_suggester.py</files>
  <action>
    Create `tests/test_blacklist_suggester.py`:
    - Test suggestion generation from recurring negative feedback notes.
    - Test existing rules are not duplicated in suggestions.
    - Test search term refinement from positive job titles.
  </action>
  <verify>pytest tests/test_blacklist_suggester.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_blacklist_suggester.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for blacklist suggester and query optimizer</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_blacklist_suggester.py` passes with zero errors
</verification>

<success_criteria>
- Blacklist suggester and search query optimizer operational and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-loop-and-search-refinement-engine/05-02-SUMMARY.md`
</output>
