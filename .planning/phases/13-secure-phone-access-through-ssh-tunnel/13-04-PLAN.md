---
phase: 13-secure-phone-access-through-ssh-tunnel
plan: 04
type: execute
wave: 1
depends_on: [13-01-PLAN.md, 13-02-PLAN.md, 13-03-PLAN.md]
autonomous: true
requirements: [REQ-REMOTE-06, REQ-REMOTE-07, REQ-REMOTE-08]
---

<objective>
Expose Tailscale SSH connection preferences and local client status in the Settings tab without granting the web application permission to execute privileged networking commands.
</objective>

<tasks>
- Add persisted Tailscale enablement, hostname/IP, SSH user, SSH port, Jobot forwarding port, and MagicDNS preferences.
- Detect local Tailscale status asynchronously with a bounded timeout.
- Render a copy-ready SSH forwarding command and explain the private-access trust boundary.
- Validate host/user inputs and port ranges, and test rendering and persistence.
</tasks>

<verification>
- Settings UI renders the Tailscale SSH panel.
- Preferences persist through `/api/settings/remote-access`.
- Unsafe command characters are rejected.
- Missing or offline Tailscale remains non-blocking and visible as status.
</verification>
