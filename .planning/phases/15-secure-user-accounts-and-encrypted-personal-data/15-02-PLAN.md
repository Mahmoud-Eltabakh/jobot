---
phase: 15-secure-user-accounts-and-encrypted-personal-data
plan: 02
type: execute
wave: 2
depends_on: [15-01-PLAN.md]
files_modified: []
autonomous: true
requirements: [REQ-ACC-03, REQ-ACC-04, REQ-SEC-01, REQ-SEC-02]
user_setup: []

must_haves:
  truths:
    - "Sensitive personal data is encrypted before persistence"
    - "Key management supports rotation and safe migration"
    - "No plaintext user secrets are stored in the database or default config"
  artifacts: []
  key_links: []
---

<objective>
Add encryption for sensitive personal job-search data and build a safe key-management pattern for local self-hosted operation.

Purpose: Protect CV content, credentials, cookies, and other personal data without sacrificing self-hosted local use.
Output: Encryption service, key management design, and secure storage patterns.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/account-login-encryption.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement encryption service</name>
  <files>app/security/, app/db/models.py</files>
  <action>Build a small cryptography layer using authenticated encryption (AES-GCM or equivalent) for sensitive fields, including user profile text, LinkedIn cookies, AI provider secrets, and any CV payloads that may contain personal information.</action>
  <verify>
    <automated>Review for encryption-before-persistence and no plaintext sensitive storage.</automated>
  </verify>
  <done>The cryptography service is defined and ready for secure storage integration.</done>
</task>

<task type="auto">
  <name>Task 2: Design key storage and rotation</name>
  <files>app/security/, app/config.py, .env.example</files>
  <action>Keep the master key in environment variables or OS key storage, version encrypted records with a key ID, and add migration hooks for rotating and re-encrypting user data safely.</action>
  <verify>
    <automated>Confirm secrets are not hard-coded and rotation is supported by design.</automated>
  </verify>
  <done>Key management and rotation pattern is in place for production local deployments.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce per-user encryption boundaries</name>
  <files>app/repositories/, app/api/, app/web/</files>
  <action>Scope encryption to the authenticated user and ensure the encryption layer is called before persisting all sensitive data, with clear handling for missing or expired keys.</action>
  <verify>
    <automated>Confirm user-sensitive fields are encrypted and scoped before save.</automated>
  </verify>
  <done>Secure, user-scoped encryption boundaries are enforced throughout the app.</done>
</task>

</tasks>

<verification>
- Confirm all sensitive fields are encrypted at rest.
- Confirm the encryption pattern prevents hard-coded secrets and supports rotation.
- Confirm the user-specific key path is protected and isolated per account.
</verification>

<success_criteria>
Sensitive account information remains encrypted and protected while remaining accessible to the correct user only.
</success_criteria>

<output>
Create `.planning/phases/15-secure-user-accounts-and-encrypted-personal-data/15-02-SUMMARY.md` when done
</output>
