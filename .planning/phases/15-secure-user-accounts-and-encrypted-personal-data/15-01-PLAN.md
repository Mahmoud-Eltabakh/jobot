---
phase: 15-secure-user-accounts-and-encrypted-personal-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-ACC-01, REQ-ACC-02, REQ-ACC-05]
user_setup: []

must_haves:
  truths:
    - "Users can create and sign in to personal accounts with secure password handling"
    - "Each authenticated user sees only their own data"
    - "Session handling is explicit and revocable"
  artifacts: []
  key_links: []
---

<objective>
Add a secure account model with user-level authentication and strict data scoping for every user-owned record.

Purpose: Isolate each person’s dashboard, profile, settings, notes, and search history while keeping Jobot self-hosted and local-first.
Output: Authentication schema and account-scoping requirements for the project.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/account-login-encryption.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define account and session schema</name>
  <files>app/db/models.py, app/db/database.py</files>
  <action>Create a `User` table with unique identity fields, password hash, status metadata, and a secure `SessionToken` or `UserSession` record with expiration and revocation fields. Add `user_id` references to the primary user-owned models.</action>
  <verify>
    <automated>Review for a unique account model and multi-user-aware table design.</automated>
  </verify>
  <done>The account and session model is defined and ready for implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authentication service</name>
  <files>app/auth/, app/api/auth.py, app/web/routes.py</files>
  <action>Build secure password hashing and verification with bcrypt/Argon2, login/logout endpoints, and user session validation for protected routes and dashboard pages.</action>
  <verify>
    <automated>Confirm password hashes are never stored in plaintext and protected routes require an authenticated user.</automated>
  </verify>
  <done>Authentication logic and session enforcement are designed for secure account login.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce account-level isolation</name>
  <files>app/repositories/, app/api/, app/web/</files>
  <action>Ensure all DB queries for jobs, profile records, notes, settings, and task status use `user_id` filters. Add a guard layer that rejects cross-user access and keeps single-user assumptions out of the codebase.</action>
  <verify>
    <automated>Confirm all user-owned data access is scoped by the logged-in account.</automated>
  </verify>
  <done>Account isolation is enforced across the application data layer and routes.</done>
</task>

</tasks>

<verification>
- Confirm each user has a unique account record and protected session lifecycle.
- Confirm password handling uses a strong hash and never stores plaintext secrets.
- Confirm all user-owned data access is scoped by `user_id` and isolated per login.
</verification>

<success_criteria>
The project supports secure local login and persistent per-user isolation without leaking data between accounts.
</success_criteria>

<output>
Create `.planning/phases/15-secure-user-accounts-and-encrypted-personal-data/15-01-SUMMARY.md` when done
</output>
