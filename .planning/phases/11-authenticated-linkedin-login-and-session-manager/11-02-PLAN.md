# Plan 11-02: Authenticated LinkedIn Profile Ingestion & Synchronizer

## Objective
Enhance `LinkedInProfileAnalyzer` to perform rich, authenticated multi-section profile DOM extraction using active session cookies.

## Tasks
1. Update `app/ai/linkedin_analyzer.py` to use `LinkedInAuthManager` cookies for authenticated navigation.
2. Extract detailed experience items, skills list, education, and headline into structured `ExtractedProfile`.
3. Re-embed and synchronize into ChromaDB cv_profile collection.
