# Plan 11-03: LinkedIn Login Web UI & Profile Sync Dialog

## Objective
Add interactive LinkedIn login controls and authentication status badges to the Profile tab.

## Tasks
1. Update `templates/components/profile.html` with an interactive "LinkedIn Account Login" form (Email + Password or direct Cookie).
2. Add HTMX endpoints in `app/web/routes.py` for `/web/profile/linkedin/login` with real-time feedback.
3. Expose session status (Logged In / Active Cookie / Expired).
