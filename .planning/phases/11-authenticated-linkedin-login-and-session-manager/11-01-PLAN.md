# Plan 11-01: Playwright Interactive Login & Session Cookie Extractor

## Objective
Implement `LinkedInAuthManager` capable of automated credential login, 2FA challenge handling, and session cookie capture.

## Tasks
1. Create `app/scrapers/linkedin_auth.py` with `LinkedInAuthManager`.
2. Implement `login_with_credentials()` and `validate_session_cookie()` methods.
3. Extract `li_at` session cookie and store in SQLite `UserProfile`.
