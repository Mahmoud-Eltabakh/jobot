# Plan 10-01: ScrapeTask Data Model & SQLite Queue Storage

## Objective
Implement `ScrapeTask` in `app/db/models.py` with full state machine transitions and migration helpers.

## Tasks
1. Add `ScrapeTask` SQLModel in `app/db/models.py`.
2. Update `migrate_sqlite_columns()` and `init_db()` in `app/db/database.py` to auto-create and index `scrape_tasks`.
3. Create `app/queue/task_queue.py` with `TaskQueue` manager (enqueue, claim_next_task, mark_completed, mark_failed, get_stats).
