---
phase: 04-modern-web-dashboard-and-job-tracker-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/web/routes.py
  - templates/components/kanban.html
  - templates/components/kanban_card.html
  - templates/components/table.html
  - tests/test_htmx_views.py
autonomous: true
requirements:
  - REQ-UI-02
  - REQ-UI-03
user_setup: []

must_haves:
  truths:
    - "Kanban board renders all 8 lifecycle stages with cards grouped by current status"
    - "Cards display title, company, location, remote badge, salary tag, and color-coded Fit Score badge"
    - "Changing job status updates SQLite database and swaps card into new column via HTMX without full page reload"
    - "Table view presents dense sortable columns with pagination"
  artifacts:
    - "templates/components/kanban.html"
    - "templates/components/kanban_card.html"
    - "templates/components/table.html"
    - "tests/test_htmx_views.py"
  key_links:
    - "Kanban and Table components interact with app.web.routes and update app.db.models.Job"
---

<objective>
Implement the interactive 8-column Kanban Board and dense Data Table views with live HTMX status transitions.

Purpose: Provide candidates with visual tracking across all application stages and high-density sorting views.
Output: Kanban template, card template, table template, and integration tests.
</objective>

<threat_model>
- **T-05 (Stored XSS)**: Strict escaping of all company names, job titles, and locations in HTML cards.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kanban Board & Card Components</name>
  <files>templates/components/kanban.html, templates/components/kanban_card.html, app/web/routes.py</files>
  <action>
    Create `templates/components/kanban.html`:
    - Responsive 8-column layout for: `seen`, `applied`, `waiting for respond`, `1. interview`, `2. interview`, `3. interview`, `not a good fit`, `rejected`.
    - Column headers with item counters and color-coded stage indicators.
    
    Create `templates/components/kanban_card.html`:
    - Card container with click trigger (`hx-get="/web/job/{id}/inspect" hx-target="#inspector-drawer"`).
    - Header: Title, Company, Source tag (LinkedIn / StepStone / Google).
    - Badges: Remote/Location, Salary tag.
    - Fit Score gauge (Emerald >= 80%, Amber 60-79%, Slate < 60%).
    - Quick Move dropdown menu (`hx-put="/api/jobs/{id}/status"` with target refresh).
    
    Add endpoint `GET /web/views/kanban` in `app/web/routes.py` querying jobs grouped by status.
    Add endpoint `PUT /api/jobs/{id}/status` in `app/web/routes.py` updating status and creating `JobStatusHistory` record.
  </action>
  <verify>python -c "assert open('templates/components/kanban.html').read().find('seen') != -1; print('Kanban template OK')"</verify>
  <acceptance_criteria>
    - GET /web/views/kanban returns HTML with all 8 column swimlanes
    - PUT /api/jobs/{id}/status updates Job status and logs status history
  </acceptance_criteria>
  <done>Kanban board and status transition endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Data Table View Component</name>
  <files>templates/components/table.html, app/web/routes.py</files>
  <action>
    Create `templates/components/table.html`:
    - Dense data table rendering rows with columns: Status, Fit Score, Job Title, Company, Location, Salary, Source, Date Posted, Actions.
    - Column header sort links (`hx-get="/web/views/table?sort_by=fit_score&sort_order=desc"`).
    - Status pill dropdown on each row.
    
    Add endpoint `GET /web/views/table` in `app/web/routes.py` supporting sorting and pagination.
  </action>
  <verify>python -c "assert open('templates/components/table.html').read().find('<table') != -1; print('Table template OK')"</verify>
  <acceptance_criteria>
    - GET /web/views/table returns table HTML with sortable headers
  </acceptance_criteria>
  <done>Data table view component implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Kanban and Table Integration Tests</name>
  <files>tests/test_htmx_views.py</files>
  <action>
    Create `tests/test_htmx_views.py`:
    - Test `GET /web/views/kanban` renders cards in appropriate columns.
    - Test `PUT /api/jobs/{id}/status` transitions job status from `seen` to `applied` and creates `JobStatusHistory`.
    - Test `GET /web/views/table` renders rows sorted by fit score.
  </action>
  <verify>pytest tests/test_htmx_views.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_htmx_views.py pass cleanly
  </acceptance_criteria>
  <done>Integration tests created and passing for Kanban and Table views</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_htmx_views.py` passes with zero errors
</verification>

<success_criteria>
- 8-column Kanban board and Data Table views implemented with HTMX reactivity and test verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-02-SUMMARY.md`
</output>
