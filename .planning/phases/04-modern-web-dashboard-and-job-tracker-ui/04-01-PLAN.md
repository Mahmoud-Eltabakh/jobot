---
phase: 04-modern-web-dashboard-and-job-tracker-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/web/__init__.py
  - app/web/routes.py
  - templates/base.html
  - templates/index.html
  - app/main.py
  - tests/test_web_routes.py
autonomous: true
requirements:
  - REQ-UI-01
user_setup: []

must_haves:
  truths:
    - "FastAPI serves server-rendered HTML at / using Jinja2 templates"
    - "Modern Dark Theme layout is configured with Tailwind CSS CDN and HTMX script tags"
    - "Base template includes navigation header with live status badges and view switcher (Kanban / Table / Settings)"
  artifacts:
    - "app/web/routes.py"
    - "templates/base.html"
    - "templates/index.html"
    - "tests/test_web_routes.py"
  key_links:
    - "app.main mounts app.web.routes router and registers Jinja2Templates at templates/"
---

<objective>
Implement the base web router, Jinja2 template environment, and modern dark layout shell.

Purpose: Provide the foundational HTML structure, Tailwind CSS theme, and HTMX integration for all UI views.
Output: Base templates, web router, and test suite.
</objective>

<threat_model>
- **T-05 (Stored / Reflected XSS)**: Jinja2 auto-escaping active on all templates; strict CSP headers configured.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jinja2 Templates Environment & Base Layout</name>
  <files>templates/base.html, templates/index.html</files>
  <action>
    Create `templates/base.html`:
    - HTML5 doctype with Dark theme (`class="dark bg-slate-950 text-slate-100"`).
    - Include Tailwind CSS CDN and HTMX 1.9+ CDN in `<head>`.
    - Navigation bar with Jobot logo, "Scrape Now" instant trigger button (`hx-post="/api/scrapers/run"`), active job count badge, and View toggles (Kanban, Table, Settings).
    - Main container with modal / drawer anchor target `#inspector-drawer`.
    
    Create `templates/index.html`:
    - Extends `base.html`.
    - Includes Filter Bar component anchor, View container anchor (`#view-container`), and default Kanban view load (`hx-get="/web/views/kanban" hx-trigger="load"`).
  </action>
  <verify>python -c "assert open('templates/base.html').read().find('htmx.org') != -1; print('base.html OK')"</verify>
  <acceptance_criteria>
    - templates/base.html and index.html exist and import Tailwind CSS and HTMX
  </acceptance_criteria>
  <done>Base layout and Jinja2 templates created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Web Routes & FastAPI Template Mount</name>
  <files>app/web/__init__.py, app/web/routes.py, app/main.py</files>
  <action>
    Create `app/web/routes.py`:
    - Define APIRouter with endpoints:
      - `GET /`: Renders `index.html` passing current user profile and stats.
      - `GET /web/stats`: Returns quick statistics (total jobs, new today, in interview).
    - Mount web router in `app/main.py`.
    - Mount `StaticFiles` if needed for static assets.
  </action>
  <verify>python -c "from app.web.routes import router; print('Web routes imported')"</verify>
  <acceptance_criteria>
    - GET / returns status code 200 with HTML content type
  </acceptance_criteria>
  <done>Web router and template rendering mounted in FastAPI</done>
</task>

<task type="auto">
  <name>Task 3: Automated Route Tests in tests/test_web_routes.py</name>
  <files>tests/test_web_routes.py</files>
  <action>
    Create `tests/test_web_routes.py`:
    - Test `GET /` returns 200 OK and contains "Jobot".
    - Test template context contains necessary configuration and stats.
  </action>
  <verify>pytest tests/test_web_routes.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_web_routes.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for base web routes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_web_routes.py` passes with zero errors
</verification>

<success_criteria>
- FastAPI Jinja2 template rendering operational with modern dark theme layout and HTMX integration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-01-SUMMARY.md`
</output>
