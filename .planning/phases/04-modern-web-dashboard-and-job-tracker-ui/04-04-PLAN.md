---
phase: 04-modern-web-dashboard-and-job-tracker-ui
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - templates/components/job_inspector.html
  - app/web/routes.py
  - tests/test_htmx_views.py
autonomous: true
requirements:
  - REQ-UI-04
  - REQ-UI-05
user_setup: []

must_haves:
  truths:
    - "Clicking a job card or table row opens the Job Inspector split-pane drawer / modal"
    - "Inspector displays complete job description, AI fit score breakdown, pros, cons, and missing skills"
    - "Provides direct link button opening external application URL in new tab"
    - "Includes candidate notes editor allowing adding notes and logging feedback"
  artifacts:
    - "templates/components/job_inspector.html"
  key_links:
    - "Job Inspector submits candidate notes to app.web.routes and updates app.db.models.FeedbackNote"
---

<objective>
Implement the Job Inspector split-pane drawer / modal with AI evaluation breakdown and candidate notes editor.

Purpose: Allow candidates to thoroughly review job details, understand why the AI scored the match, open external applications, and record feedback notes.
Output: Job inspector template, notes submission API, and test suite.
</objective>

<threat_model>
- **T-04 (Safe Outbound Links)**: Ensure job application URLs are validated against HTTP/HTTPS protocols.
- **T-05 (XSS in Notes)**: Escape candidate notes and descriptions in inspector modal.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Job Inspector Component Template</name>
  <files>templates/components/job_inspector.html, app/web/routes.py</files>
  <action>
    Create `templates/components/job_inspector.html`:
    - Slide-over drawer / modal container targeting `#inspector-drawer`.
    - Header: Job Title, Company, Location, Compensation, Close button.
    - Action Buttons: Direct Apply Link (`target="_blank"`), Status selector, Re-evaluate with AI button (`hx-post="/api/jobs/{id}/re-evaluate"`).
    - Left Pane: Full job description with preserved formatting.
    - Right Pane:
      1. AI Evaluation card (0-100% Score dial, Recommendation badge, Summary, Pros list, Cons list, Missing Skills list).
      2. Candidate Notes & Feedback Form (`hx-post="/api/jobs/{id}/notes"`): Note textarea with sentiment selector (Positive, Neutral, Negative).
      3. Status Transition History log timeline.
    
    Add endpoint `GET /web/job/{id}/inspect` in `app/web/routes.py` fetching Job, parsed JSON lists, and history.
    Add endpoint `POST /api/jobs/{id}/notes` saving new `FeedbackNote` and updating status history.
  </action>
  <verify>python -c "assert open('templates/components/job_inspector.html').read().find('fit_score') != -1; print('Inspector template OK')"</verify>
  <acceptance_criteria>
    - GET /web/job/{id}/inspect renders HTML with AI match breakdown and notes editor
    - POST /api/jobs/{id}/notes creates FeedbackNote in database
  </acceptance_criteria>
  <done>Job inspector component and notes API implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add Inspector Tests to tests/test_htmx_views.py</name>
  <files>tests/test_htmx_views.py</files>
  <action>
    Update `tests/test_htmx_views.py`:
    - Test `GET /web/job/{id}/inspect` returns 200 and renders AI pros, cons, and score.
    - Test `POST /api/jobs/{id}/notes` creates a note and re-renders notes timeline.
  </action>
  <verify>pytest tests/test_htmx_views.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_htmx_views.py pass cleanly
  </acceptance_criteria>
  <done>Job inspector integration tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_htmx_views.py` passes with zero errors
</verification>

<success_criteria>
- Job Inspector detail drawer and candidate notes editor operational with test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-04-SUMMARY.md`
</output>
