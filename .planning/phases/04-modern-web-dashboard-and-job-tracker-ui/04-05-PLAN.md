---
phase: 04-modern-web-dashboard-and-job-tracker-ui
plan: 05
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - templates/components/settings.html
  - app/api/settings.py
  - app/web/routes.py
  - tests/test_web_routes.py
autonomous: true
requirements:
  - REQ-CFG-01
  - REQ-CFG-02
  - REQ-CFG-03
user_setup: []

must_haves:
  truths:
    - "Settings panel provides UI toggle between Local Ollama and Cloud API providers"
    - "Provides real-time AI Connection Test button with status indicator badge"
    - "Allows managing Title, Company, and Keyword Blacklist rules (add, toggle active, delete)"
    - "Allows uploading new CV PDF/DOCX to re-extract and re-embed candidate profile"
  artifacts:
    - "templates/components/settings.html"
    - "app/api/settings.py"
  key_links:
    - "Settings panel reads and updates app.db.models.AppSettings, FilterRule, and UserProfile"
---

<objective>
Implement the Settings panel with AI Provider Configurator, Blacklist Manager, and CV Re-upload Form.

Purpose: Allow candidates to configure AI backends, test connections, manage blacklist rules, and upload updated resumes directly from the browser without editing config files.
Output: Settings view template, settings API endpoints, and test suite.
</objective>

<threat_model>
- **T-07 (API Key & Host Isolation)**: Sanitize base URLs; store API keys securely in SQLite AppSettings without exposing keys in public logs.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings API Router in app/api/settings.py</name>
  <files>app/api/settings.py, app/main.py</files>
  <action>
    Create `app/api/settings.py`:
    - `GET /api/settings`: Returns current configuration (provider, models, intervals, rules).
    - `POST /api/settings/ai`: Updates AI provider settings in `AppSettings` (`ai_provider`, `ollama_model`, `ollama_base_url`, `openai_api_key`, `openai_model`).
    - `POST /api/settings/test-ai`: Instantiates client and executes `check_health()`, returning JSON status and latency.
    - `POST /api/settings/rules`: Creates new `FilterRule` (type: title/keyword/company).
    - `DELETE /api/settings/rules/{id}`: Deletes `FilterRule`.
    - `POST /api/settings/upload-cv`: Accepts uploaded PDF/DOCX file, parses via `CVParser`, extracts `ExtractedProfile`, saves to `UserProfile`, and embeds via `ProfileEmbedder`.
    
    Mount `app.api.settings.router` in `app/main.py`.
  </action>
  <verify>python -c "from app.api.settings import router; print('Settings router imported')"</verify>
  <acceptance_criteria>
    - Settings router exposes AI config, test connection, filter rule CRUD, and CV upload endpoints
  </acceptance_criteria>
  <done>Settings API router implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Settings View Template in templates/components/settings.html</name>
  <files>templates/components/settings.html, app/web/routes.py</files>
  <action>
    Create `templates/components/settings.html`:
    - Section 1: **AI Provider Configurator**
      - Toggle radio buttons: `Local Ollama (Offline)` vs `Cloud API (OpenAI / Groq / Custom)`.
      - Conditional inputs for Ollama Base URL, Ollama Model / OpenAI API Key, Base URL, Model.
      - "Test Connection" button (`hx-post="/api/settings/test-ai" hx-target="#ai-status-badge"`).
    - Section 2: **CV & Profile Management**
      - Current candidate summary & skills badge list.
      - CV Upload form (`hx-post="/api/settings/upload-cv"` with multipart encoding).
    - Section 3: **Blacklist & Exclusion Rules**
      - Table of active rules with delete buttons.
      - Quick Add rule form (Rule Type selector, Pattern input, Add button).
    
    Add endpoint `GET /web/views/settings` in `app/web/routes.py` rendering `templates/components/settings.html`.
  </action>
  <verify>python -c "assert open('templates/components/settings.html').read().find('ai_provider') != -1; print('Settings template OK')"</verify>
  <acceptance_criteria>
    - GET /web/views/settings returns HTML with AI configurator, CV manager, and blacklist tables
  </acceptance_criteria>
  <done>Settings component template created and connected</done>
</task>

<task type="auto">
  <name>Task 3: Automated Settings Tests in tests/test_web_routes.py</name>
  <files>tests/test_web_routes.py</files>
  <action>
    Update `tests/test_web_routes.py`:
    - Test `GET /web/views/settings` returns 200 and contains settings sections.
    - Test `POST /api/settings/ai` updates AppSettings in SQLite.
    - Test `POST /api/settings/rules` creates a FilterRule.
    - Test `POST /api/settings/test-ai` returns connection health status.
  </action>
  <verify>pytest tests/test_web_routes.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_web_routes.py pass cleanly
  </acceptance_criteria>
  <done>Settings tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_web_routes.py` passes with zero errors
- [ ] `pytest` passes all Phase 1-4 tests
</verification>

<success_criteria>
- Settings panel with live AI provider toggle, healthcheck test button, blacklist manager, and CV uploader operational and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-05-SUMMARY.md`
</output>
