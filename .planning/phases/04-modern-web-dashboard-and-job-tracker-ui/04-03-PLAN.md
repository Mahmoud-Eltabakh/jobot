---
phase: 04-modern-web-dashboard-and-job-tracker-ui
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - templates/components/filter_bar.html
  - app/web/routes.py
  - tests/test_ui_filters.py
autonomous: true
requirements:
  - REQ-UI-03
user_setup: []

must_haves:
  truths:
    - "Filter bar provides dynamic Fit Score slider, Remote/Workplace pills, salary filter, and source checkboxes"
    - "Search input triggers debounced HTMX request updating the active view in real-time"
    - "Quick toggles allow hiding rejected and not-a-good-fit jobs instantly"
  artifacts:
    - "templates/components/filter_bar.html"
    - "tests/test_ui_filters.py"
  key_links:
    - "templates/components/filter_bar.html sends filter query params to app.web.routes"
---

<objective>
Implement the Multi-Criteria Sorting & Filtering Toolbar with live HTMX reactive updates.

Purpose: Allow candidates to instantly filter hundreds of scraped positions by match quality, remote status, compensation, source, and keywords.
Output: Filter bar component template, filter query handler, and test suite.
</objective>

<threat_model>
- **T-03 (SQL Query Injection in Filters)**: Parameterized SQLModel queries filtering on min_score, is_remote, search query, and statuses.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-01-PLAN.md
@.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Filter Bar Component Template</name>
  <files>templates/components/filter_bar.html</files>
  <action>
    Create `templates/components/filter_bar.html`:
    - Search input: text search across title, company, description with `hx-trigger="keyup changed delay:300ms, search"`.
    - Min Fit Score Slider: Range input (0 to 90, step 10) with live score display badge.
    - Work Model Pills: Buttons for `All`, `Remote`, `Hybrid`, `On-site`.
    - Min Salary Input: Numeric input field.
    - Source checkboxes: `LinkedIn`, `StepStone`, `Google`.
    - Quick Toggles: Checkboxes for `"Hide Rejected"` and `"Hide Not a Fit"`.
    - Form wrapper targeting `#view-container` using `hx-include="closest form"`.
  </action>
  <verify>python -c "assert open('templates/components/filter_bar.html').read().find('min_score') != -1; print('Filter bar template OK')"</verify>
  <acceptance_criteria>
    - templates/components/filter_bar.html defines all required filter controls
  </acceptance_criteria>
  <done>Filter bar component template created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Filter Query Resolver in app/web/routes.py</name>
  <files>app/web/routes.py</files>
  <action>
    Update `app/web/routes.py`:
    - Implement helper `apply_job_filters(query, q, min_score, is_remote, min_salary, sources, hide_rejected, hide_not_fit, sort_by, sort_order)`.
    - Connect filter resolver to both `GET /web/views/kanban` and `GET /web/views/table`.
  </action>
  <verify>python -c "from app.web.routes import apply_job_filters; print('Filter resolver OK')"</verify>
  <acceptance_criteria>
    - apply_job_filters applies WHERE clauses dynamically based on passed query parameters
  </acceptance_criteria>
  <done>Filter query resolver integrated into web routes</done>
</task>

<task type="auto">
  <name>Task 3: Automated Filter Tests in tests/test_ui_filters.py</name>
  <files>tests/test_ui_filters.py</files>
  <action>
    Create `tests/test_ui_filters.py`:
    - Test filtering by search keyword (`q=Python`).
    - Test filtering by minimum fit score (`min_score=80`).
    - Test filtering by remote status (`is_remote=true`).
    - Test hiding rejected / not a fit jobs.
    - Test sorting by fit score ascending/descending.
  </action>
  <verify>pytest tests/test_ui_filters.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_ui_filters.py pass cleanly
  </acceptance_criteria>
  <done>Unit tests created and passing for UI filtering</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_ui_filters.py` passes with zero errors
</verification>

<success_criteria>
- Multi-criteria filter toolbar and backend query resolver operational and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modern-web-dashboard-and-job-tracker-ui/04-03-SUMMARY.md`
</output>
