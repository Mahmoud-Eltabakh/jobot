---
phase: 01-core-foundation-and-data-architecture
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/db/vector.py
  - tests/test_vector.py
autonomous: true
requirements:
  - REQ-DB-02
user_setup: []

must_haves:
  truths:
    - "ChromaDB client initializes in persistent mode at configured storage directory"
    - "Vector store provides dedicated collections for cv_profile, job_descriptions, and user_feedback"
    - "Vector store helper provides upsert, similarity query, delete, and count operations"
  artifacts:
    - "app/db/vector.py"
    - "tests/test_vector.py"
  key_links:
    - "app.db.vector imports settings to configure ChromaDB PersistentClient path and embedding defaults"
---

<objective>
Implement the embedded ChromaDB vector store module to manage semantic embeddings for candidate profiles, job descriptions, and user feedback vectors.

Purpose: Provide high-speed, local vector search and RAG capabilities without requiring external vector DB services or cloud APIs.
Output: Vector store manager class with collection abstractions and comprehensive test suite.
</objective>

<threat_model>
- **T-05 (Local Storage Path Isolation)**: Ensure ChromaDB persistence directory is kept inside the configured application storage path without path traversal vulnerabilities.
- **T-01 (Vector Metadata Validation)**: Restrict metadata payloads to validated primitive types (strings, ints, floats, bools) preventing injection.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation-and-data-architecture/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement VectorStore manager in app/db/vector.py</name>
  <files>app/db/vector.py</files>
  <action>
    Implement `VectorStore` class wrapping `chromadb.PersistentClient`:
    - Auto-create storage directory if non-existent (`settings.chroma_dir`).
    - Expose get_or_create collection methods for:
      1. `cv_profile`: for candidate CV chunks and skill summaries.
      2. `job_descriptions`: for scraped job posts (storing metadata: `job_id`, `company`, `title`, `source`).
      3. `user_feedback`: for notes and sentiment vectors.
    - Provide helper methods:
      - `upsert_documents(collection_name, ids, documents, metadatas, embeddings=None)`
      - `query_similar(collection_name, query_texts=None, query_embeddings=None, n_results=5, where=None)`
      - `delete_documents(collection_name, ids)`
      - `count(collection_name)`
      - `reset_collection(collection_name)`
  </action>
  <verify>python -c "from app.db.vector import get_vector_store; vs = get_vector_store(); print('Vector store initialized')"</verify>
  <acceptance_criteria>
    - VectorStore handles persistent storage path creation
    - Collections cv_profile, job_descriptions, and user_feedback can be initialized
  </acceptance_criteria>
  <done>Vector store client and collection manager implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement vector store unit and integration tests</name>
  <files>tests/test_vector.py</files>
  <action>
    Create `tests/test_vector.py` using a temporary directory for ChromaDB:
    - Test upserting sample documents into `cv_profile` and `job_descriptions`.
    - Test semantic query similarity retrieval and metadata filtering.
    - Test document deletion and count verification.
    - Test error handling when querying non-existent collections.
  </action>
  <verify>pytest tests/test_vector.py</verify>
  <acceptance_criteria>
    - All tests in tests/test_vector.py pass cleanly
  </acceptance_criteria>
  <done>Vector store operations verified with automated tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_vector.py` passes
- [ ] Full test suite `pytest` passes
</verification>

<success_criteria>
- ChromaDB persistent vector store operational with collection lifecycle helpers and verified query capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-and-data-architecture/01-03-SUMMARY.md`
</output>
