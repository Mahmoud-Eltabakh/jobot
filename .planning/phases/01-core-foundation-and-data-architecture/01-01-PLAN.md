---
phase: 01-core-foundation-and-data-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .env.example
  - .dockerignore
  - Dockerfile.dev
  - docker-compose.dev.yml
  - app/__init__.py
  - app/main.py
  - app/core/__init__.py
  - app/core/config.py
  - app/core/logging.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_main.py
autonomous: true
requirements:
  - REQ-DB-01
  - REQ-DB-03
  - REQ-OPS-01
  - REQ-OPS-02
user_setup: []

must_haves:
  truths:
    - "FastAPI application initializes and responds to /health endpoint with JSON status"
    - "Pydantic Settings correctly loads default configurations and overrides via .env"
    - "Logging system outputs clean structured logs with configurable levels"
    - "Development Docker Compose configuration allows running and live-reloading Jobot and Ollama containers"
  artifacts:
    - "pyproject.toml"
    - "requirements.txt"
    - ".env.example"
    - ".dockerignore"
    - "Dockerfile.dev"
    - "docker-compose.dev.yml"
    - "app/main.py"
    - "app/core/config.py"
    - "app/core/logging.py"
  key_links:
    - "app.main imports app.core.config.get_settings to configure application metadata and lifespan"
---

<objective>
Establish the Python project structure, dependency specifications, centralized Pydantic settings, structured logging, and FastAPI entry point.

Purpose: Provide the clean, standardized runtime foundation required by database, AI, scraper, and web dashboard modules.
Output: Working FastAPI project with environment configuration, test infrastructure, and healthcheck route.
</objective>

<threat_model>
- **T-01 (Config / Host Exposure)**: Restrict default host bindings to localhost (`127.0.0.1`) in settings.
- **T-05 (CORS / Headers)**: Configure safe headers and local CORS policy in FastAPI application.
</threat_model>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation-and-data-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project packaging, dependency files, and environment template</name>
  <files>pyproject.toml, requirements.txt, .env.example</files>
  <action>
    Create pyproject.toml with project metadata, dependencies (fastapi, uvicorn, sqlmodel, chromadb, pydantic-settings, python-jobspy, playwright, pypdf, pdfplumber, apscheduler, jinja2, httpx, pytest, pytest-asyncio), and tool configurations for pytest and ruff.
    Create requirements.txt locking key dependencies.
    Create .env.example with documented settings (DATABASE_URL, CHROMA_DIR, OLLAMA_BASE_URL, OLLAMA_MODEL, OLLAMA_EMBED_MODEL, LOG_LEVEL, PORT).
  </action>
  <verify>python -c "import tomllib; tomllib.loads(open('pyproject.toml', 'rb').read().decode('utf-8'))"</verify>
  <acceptance_criteria>
    - pyproject.toml is valid TOML and specifies python >= 3.10
    - .env.example contains default keys for DATABASE_URL and OLLAMA_BASE_URL
  </acceptance_criteria>
  <done>Project metadata and dependency manifests exist and parse successfully</done>
</task>

<task type="auto">
  <name>Task 2: Implement centralized settings and structured logging in app/core</name>
  <files>app/__init__.py, app/core/__init__.py, app/core/config.py, app/core/logging.py</files>
  <action>
    Implement Settings using `pydantic_settings.BaseSettings` with `SettingsConfigDict(env_file=".env", extra="ignore")`.
    Include settings for app_name, env, debug, port, database_url (defaulting to "sqlite:///data/jobot.db"), chroma_dir (defaulting to "data/chroma"), ollama_base_url (defaulting to "http://localhost:11434"), ollama_model (defaulting to "llama3.1:8b"), ollama_embed_model (defaulting to "nomic-embed-text"), log_level (defaulting to "INFO").
    Implement `setup_logging` in app/core/logging.py configuring standard library logging with formatting for timestamp, level, and module.
  </action>
  <verify>python -c "from app.core.config import get_settings; s = get_settings(); assert s.app_name == 'Jobot'"</verify>
  <acceptance_criteria>
    - app/core/config.py defines Settings and get_settings() with lru_cache
    - app/core/logging.py exports setup_logging(level)
  </acceptance_criteria>
  <done>Settings and logging modules importable and configured with sensible defaults</done>
</task>

<task type="auto">
  <name>Task 3: Implement FastAPI application entry point with lifespan and health endpoint</name>
  <files>app/main.py, tests/__init__.py, tests/conftest.py, tests/test_config.py, tests/test_main.py</files>
  <action>
    Create FastAPI application in `app/main.py` with `@asynccontextmanager` lifespan managing startup and shutdown logs.
    Add `GET /health` returning JSON `{"status": "ok", "app": settings.app_name, "version": "0.1.0"}`.
    Add test fixtures in `tests/conftest.py` using `httpx.AsyncClient` or `starlette.testclient.TestClient`.
    Add `tests/test_config.py` testing settings resolution and `tests/test_main.py` testing `/health`.
  </action>
  <verify>pytest tests/test_config.py tests/test_main.py</verify>
  <acceptance_criteria>
    - GET /health returns status code 200 with status ok
    - Tests pass cleanly under pytest
  </acceptance_criteria>
  <done>FastAPI application initializes and passes health check tests</done>
</task>

<task type="auto">
  <name>Task 4: Add Docker development environment files</name>
  <files>Dockerfile.dev, docker-compose.dev.yml, .dockerignore</files>
  <action>
    Create `.dockerignore` ignoring virtual environments, caches, `.git`, and tests data.
    Create `Dockerfile.dev` based on `python:3.11-slim` installing system dependencies for Playwright Chromium, requirements, and running `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`.
    Create `docker-compose.dev.yml` configuring `jobot` (with source bind mount `./:/app`, port 8000:8000, environment linking to Ollama) and `ollama` (with persistent volume `ollama_data:/root/.ollama` and optional GPU passthrough).
  </action>
  <verify>docker compose -f docker-compose.dev.yml config</verify>
  <acceptance_criteria>
    - docker-compose.dev.yml is valid YAML and defines jobot and ollama services
    - Dockerfile.dev installs Python dependencies and configures reload entrypoint
  </acceptance_criteria>
  <done>Development Docker setup created for containerized local execution</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_config.py tests/test_main.py` passes with zero errors
- [ ] `python -m app.main` or `uvicorn app.main:app` can be parsed without syntax or import errors
</verification>

<success_criteria>
- Project dependency files, settings loader, logging system, and FastAPI application entry point are operational and verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-and-data-architecture/01-01-SUMMARY.md`
</output>
