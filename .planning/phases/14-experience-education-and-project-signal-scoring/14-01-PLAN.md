---
phase: 14-experience-education-and-project-signal-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-SIGNAL-01, REQ-SIGNAL-03, REQ-SIGNAL-06]
user_setup: []

must_haves:
  truths:
    - "The fit score includes historical skills, education, and project evidence as explicit scoring signals"
    - "The score remains transparent and user-configurable"
    - "No hard-coded static weights are introduced for the new profile-history factors"
  artifacts: []
  key_links: []
---

<objective>
Design a profile-aware scoring model that incorporates previous skills, education, and project experience as first-class fit signals without hard-coded values.

Purpose: Make Jobot rank candidates based on the full professional record rather than only current keyword overlap.
Output: A scoring specification and calibration plan for the new signal model.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the new signal taxonomy</name>
  <files>app/ai/evaluator.py, app/db/models.py</files>
  <action>Map the scoring dimensions for current skills, prior skills, education relevance, project evidence, and seniority progression. Ensure the system expresses these as structured profile signals rather than raw text fragments.</action>
  <verify>
    <automated>Review for explicit scoring categories and no hard-coded profile-history values.</automated>
  </verify>
  <done>The scoring taxonomy is defined and ready for implementation in the evaluation pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Productionize weighted calibration</name>
  <files>app/api/settings.py, app/web/routes.py</files>
  <action>Define how the calibration UI can expose weights for historical skill overlap, education relevance, project impact, and experience progression using persisted settings rather than static formulas.</action>
  <verify>
    <automated>Confirm calibration values are editable and persisted, not hard-coded.</automated>
  </verify>
  <done>The calibration system is designed to make the new profile-history factors adjustable by the user.</done>
</task>

<task type="auto">
  <name>Task 3: Verification and traceability</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>Record how the fit score will remain explainable, with a clear breakdown of which profile signals contributed to the score and why filtering decisions were made.</action>
  <verify>
    <automated>Confirm the scoring model is explainable and traceable.</automated>
  </verify>
  <done>The milestone defines a transparent, profile-aware scoring path with user control.</done>
</task>

</tasks>

<verification>
- Confirm historical skills, education, and project data are treated as first-class fit signals.
- Confirm the scoring can be calibrated through settings and remains explainable.
- Confirm no hard-coded values are embedded in the scoring logic for these new factors.
</verification>

<success_criteria>
The new scoring model clearly uses profile-history evidence (skills, education, projects) in a configurable, explainable, and non-static mechanism.
</success_criteria>

<output>
Create `.planning/phases/14-experience-education-and-project-signal-scoring/14-01-SUMMARY.md` when done
</output>
