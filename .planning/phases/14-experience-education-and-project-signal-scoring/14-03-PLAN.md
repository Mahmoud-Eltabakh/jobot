---
phase: 14-experience-education-and-project-signal-scoring
plan: 03
type: execute
wave: 1
depends_on: [14-01, 14-02]
files_modified: []
autonomous: true
requirements: [REQ-SIGNAL-01, REQ-SIGNAL-03, REQ-SIGNAL-05, REQ-SIGNAL-06]
user_setup: []

must_haves:
  truths:
    - "Jobs are rescoreable using the joined profile history model"
    - "The calibration UI exposes the new score weights"
    - "Filtering results reflect the updated scoring model"
  artifacts: []
  key_links: []
---

<objective>
Update the actual scoring and filtering execution to reflect the new history-aware profile signals and verify the behavior end to end.

Purpose: Ensure the model not only stores the historic profile evidence but actually uses it to rank jobs and filter results.
Output: A live scoring pipeline and verification checklist for the new profile-history model.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update fit-score evaluation</name>
  <files>app/ai/evaluator.py</files>
  <action>Implement the scoring aggregation for prior skills, education relevance, project experience, and seniority trajectory using persisted settings weights to replace any static assumptions.</action>
  <verify>
    <automated>Confirm score calculations include the new profile-history factors and remain configurable.</automated>
  </verify>
  <done>The evaluator incorporates historical profile evidence into the score.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the calibration layer</name>
  <files>app/api/settings.py, app/web/routes.py, templates/components/settings.html</files>
  <action>Expose the new profile-history weights in the settings controls so users can tune the balance among skills, education, project signal, and experience.</action>
  <verify>
    <automated>Confirm the UI exposes user-tunable weights for the new factors.</automated>
  </verify>
  <done>Profile-history calibration is live and editable.</done>
</task>

<task type="auto">
  <name>Task 3: Validate end-to-end scoring</name>
  <files>tests/, app/ai/evaluator.py, app/web/routes.py</files>
  <action>Run focused regression tests for the recalculated score and filtering pipeline, and confirm that jobs are filtered and listed only after profile-aware scoring.</action>
  <verify>
    <automated>Run relevant pytest checks to validate the new behavior.</automated>
  </verify>
  <done>The scoring model is validated for profile-aware ranking and filtering.</done>
</task>

</tasks>

<verification>
- Confirm normalized scoring captures profile-history evidence.
- Confirm user-configurable weights are active in the settings flow.
- Confirm the rescore + filter path works after the scoring change.
</verification>

<success_criteria>
The combined scoring system uses the joined profile evidence (skills, education, experience, projects) and allows users to tune the weights without static hard-coding.
</success_criteria>

<output>
Create `.planning/phases/14-experience-education-and-project-signal-scoring/14-03-SUMMARY.md` when done
</output>
